# Local to the learning site. The repository root .gitignore also covers
# node_modules/ and dist/, but keeping these here makes the boundary explicit.

node_modules/
dist/
.vite/
*.local

# Generated by scripts/build-content.mjs from career-roadmaps/**.md.
# Never committed: the Markdown is the single source of truth (docs/CONTENT-SCHEMA.md).
# Regenerated by `npm run build:content`, which runs automatically before dev and build.
src/data/generated/