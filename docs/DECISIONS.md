# Decisions

A lightweight decision log (ADR-style). Newest first.

## D-031 — The browser matrix names its engine, and a named engine that is missing fails

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** `browser-check.mjs` was written to cover what no string render can see — layout geometry, computed CSS, media-query behaviour, focus survival across keystrokes — and it does: 79 checks. But every run of it, local and CI, measured **Chromium**. Locally `BROWSERS` tried Edge first; in CI the runner's `/usr/bin/google-chrome` was used. The checkpoint recorded this as "two engines, not a cross-browser matrix", which was generous: two builds of one engine are two data points about one renderer, and a defect appearing only in Gecko or WebKit was invisible to **every guard in the repository**.
  - The file already had the right instinct one level down. It printed `Browser.getVersion` and asserted on the product string, because "the file's browser list tries Edge before Chrome, so on a machine with both, every run silently measured Edge while the report said 'a real browser'". That fix made the engine **visible**; it did not make a second engine **run**.
- **Decision:** Add Gecko as a first-class engine, make the engine **selectable by name** through `BROWSER_ENGINE`, and run the check as a **CI matrix** with one leg per engine.
  - **A leg that names an engine and cannot find it FAILS — even outside strict mode.** This is the load-bearing part. Without it, a leg titled `firefox` whose Firefox was missing from the image would fall through the path list, find Chromium, and report a green Firefox result. That is a claim about an engine that was never started, and it is worse than no matrix because the result would be cited as cross-browser evidence. The script asserts the *requested* engine is the one that answered.
  - **The two engines need different flags and a different resize mechanism**, and that difference is handled rather than papered over. Chromium takes `--headless=new`; Gecko takes `-headless`. Chromium resizes through `Emulation.setDeviceMetricsOverride`; **Gecko does not implement it and answers with a protocol error**, so the width band is driven through `Browser.setWindowBounds` instead. That is not a workaround for a missing feature — `matchMedia` in Firefox reads the real window, so window bounds are the mechanism the layout is genuinely computed from there.
  - **`auto` preserves the previous behaviour.** With no `BROWSER_ENGINE` set, resolution order is unchanged (Chromium first), so every existing invocation and the local workflow behave exactly as before.
- **Consequences:**
  - **WebKit remains an honest gap, and is named rather than implied.** It has no headless build a runner can install without a dependency rule 3 forbids. The matrix is two engines, which is a matrix and not complete coverage — the wording in `CHECKPOINT.md` now says so instead of implying the question is closed.
  - **Local verification is partial by necessity, and that is recorded.** This machine has Edge and no Firefox and no Chrome, so the Firefox path could be verified only as far as: it resolves the engine by name, it **fails** when the named engine is absent (observed, exit 1), and the Chromium path still passes 79 checks with the new engine-aware code. The Gecko run itself is observable only in CI. Claiming otherwise would be the exact defect this record is about.
  - The dependency-free property survives: no new package, no driver binary, no Puppeteer. A browser is found on the system and driven over the DevTools Protocol with Node 24's global `WebSocket`.

## D-030 — A phase travels on its own, and the import can only ever add

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** D-016 gave the reader a whole-profile backup across every registered storage key, and it is the right answer for a new laptop. It is the wrong answer for the case that actually recurs on a 34–112 week plan: a reader working through **one phase** on a work machine, a library PC, or a laptop that is not the one their profile lives on. Exporting all ten keys there would produce a document whose only safe use is to overwrite a profile the reader is not looking at.
- **Decision:** `exportPhase` narrows a backup to the ids one phase owns, and `importPhase` merges it back **additively**.
  - **The emitted file is a real backup** — same `format`, same `version`, same `data` map keyed by storage key, carrying only a `kind` marker. So `inspect` and `importAll` read it with **no special case**. This is the property that makes the feature safe to add: a phase file cannot drift from a full backup, because it is the same document with a narrower `data`. A separate format would have meant a second validator and a second merge rule to keep in step.
  - **Only four keys are phase-shaped, and the other six are deliberately absent rather than exported and ignored.** `progress` (filtered by the phase's own checklist and task ids), `lesson-sections` (filtered by `phaseId#` prefix), `notes` (the phase's entry, note and answers together), and `reading` — the last only when this phase is genuinely the one being read, because exporting it otherwise would drag a false "you were here" to another machine. Portfolio, applications, schedule and the three preferences describe a *profile*, not a phase.
  - **The import can never remove or overwrite.** Objects union with the **existing entry winning on a collision**, matching `mergeValue`'s rule for the full import and for the same reason: the machine the reader is sitting at is the one whose writing is current, and importing twice must be a no-op. It offers no Replace, because `importAll` already owns the destructive path behind a confirmation.
  - **An unreadable existing value is refused rather than overwritten.** The reader's writing is the one thing in the store that cannot be regenerated from the curriculum, so a merge into something this version cannot parse fails loudly instead of guessing.
- **Consequences:**
  - **The test caught a real inversion before it shipped, which is the argument for writing it.** `test-phase-transfer.mjs` asserts 56 checks and one of them failed on the first run: the spread was ordered `{ ...existing, ...incoming }`, so an import silently **replaced** the note the reader could see with a staler one from a file — the one direction this feature must not have, and invisible in the UI, which reported success. The comment above the code already said "existing wins"; the code did the opposite. A guard that encodes the intended rule in an assertion is what caught the difference between the two.
  - **Additivity is a design constraint, not a nicety**, and it is why this is an inline collapsed panel rather than a modal like `DataTransfer`. The full-profile import can overwrite ten keys at once and warrants a confirmation; this one cannot harm the machine it lands on, so it does not need to interrupt the page.
  - Guarded by `test:phase-transfer` (56 checks) in the site job of CI, weighted toward rejection and merge cases rather than the round trip, following `test-data.mjs`.

## D-029 — A shared document is reachable by name, and the rail links to it

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** D-020 made `career-roadmaps/shared/` reachable from the site for the first time, through a view reached only by clicking a sidebar entry and then a chip. `Shared.jsx` already took an `initialId` prop and read it purely as a `useState` seed — the hook a real deep-link needs, with nothing using it. The roadmap recorded the gap as "the view is currently reached only by clicking the sidebar entry and then a chip".
- **Decision:** Make the open document a **view parameter owned by `App`**, and put a real link to each document on the dashboard's reference rail.
  - **The state moved up because `Shared` is remounted on every view change.** Local state there would forget the reader's choice the moment they navigated away and back. `App` holds `sharedDocId`, passes it down as the seed, and receives the reader's own clicks back through `onSelect`.
  - **The split of ownership is the decision.** `initialId` is the **seed** — read at mount and never again, because a prop that re-asserted itself would fight the reader's clicks. `onSelect` is the **echo** — it goes up so the parent can hand the same id back if the view remounts. Without the echo, following a link to the resource list, leaving, and returning through the sidebar would silently reopen the first document.
  - **The bare "Shared" entry clears the parameter**, so a reader who clicks it lands on the first document rather than on whichever one they last followed a link to.
  - **This is still not routing.** There is no URL, no history entry, and the browser's back button still does not move between views — D-007 stands. A view parameter is local state that one page happens to read.
- **Consequences:**
  - The rail carries a **"Beside the tracks"** card listing the documents by their own titles, read from the generated `shared.json` rather than hardcoded, so adding a document to `shared/` surfaces it without touching `Dashboard.jsx`.
  - The three shared documents now have a path from the page a reader opens every session, which matters most for the anti-burnout rules — written for a reader at risk of stopping, who was previously the least likely to find them.

## D-028 — The acronym *detector* gates; the acronym *corpus report* does not

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** `scripts/audit-terms.mjs` was left open as "either find a formulation that can gate or delete the output", and it had been through five measurement passes without converging (365 → 232 → 232 → 227 domain terms). It printed a report and **always exited 0**. Reading its actual output settled the question in a way five measurement passes had not: several of its findings printed their own refutation. `**SPF** (Sender Policy Framework)` was reported as *never expanded* on a line where the expansion is plainly visible, and the same was true of `UID`, `CIDR`, `NOC`, and `GRC`.
  - Four shapes were invisible, each for a different reason: **Markdown emphasis** sat between the term and its gloss, so `SPF\s*\(` could never match `**SPF** (`; a **noun** sat between them in `CIDR notation (Classless Inter-Domain Routing)`, which no pattern allowed; the **apposition** pattern required a capital straight after the dash, so the article form the curriculum actually writes (`NOC — a Network Operations Centre`) was missed; and an **em-dash inside the gloss** was outside the character class in `UID (user ID — the number Linux uses)`. Table-cell expansions — `| **GRC** | Governance, Risk, and Compliance |` — put the gloss in a neighbouring cell with no parenthesis anywhere.
  - **This is the third instance of one defect class in this repository.** `audit-refs.mjs` had a `FORWARD_AS_PRIOR` detector that sliced `line.slice(0, m.index + 4)` — four characters from the match, i.e. `"Phas"` — so its cue regex could never fire and its historical `(0)` was not a clean corpus. `audit-time-budget.mjs` had a class that tracked "am I inside a table right now" and therefore never cross-checked a single week count. Both printed clean. A detector that cannot fire is indistinguishable from a clean corpus in its output, and that is the failure mode D-022 exists to name.
- **Decision:** **The judgement is split, and the split is the decision.** Whether a beginner can decode `SSID` unaided is a claim about *the reader's state*, so by D-022 the corpus report stays a measurement that always exits 0. Whether a definition is *present on the line* is a claim about *the text agreeing with itself*, so the **instrument** is a real check and now gates.
  - The expansion detector carries **16 controls** — 6 must-fire, 10 must-not-fire — that run on **every** invocation, and a failure prints `ACRONYM AUDIT — SELF-TEST FAILED` and exits 1 **before any finding is printed**. `--self-test` runs only the controls, and that is the form CI uses.
  - The table-cell shape is tested **by its initials, not by its shape**: the capitalised words of the next cell, minus joining words, must spell the acronym. `Governance, Risk, and Compliance` → `GRC` fires; `Collects logs from many sources` for `SIEM` does not. A loose "a capitalised phrase follows" pattern would have accepted the second and declared an explained term a finding.
- **Consequences:**
  - **The controls paid for themselves on the first run.** Wired in and executed, they failed immediately on the `CIDR notation` shape — a pattern I had not written — and stopped the audit rather than under-reporting quietly. That is the whole argument for controls in one observation.
  - **CI runs `--self-test`, never the plain form.** The plain form always exits 0, so wiring it in would add a step whose pass path is identical to a check that never ran. This is the same rule as `BROWSER_CHECK_STRICT=1` and `MIN_CHECKS` in D-023.
  - **The counts moved as false findings disappeared**: expanded at first use 601 → 613, never expanded 351 → 337, domain 270 → 256, actionable 82 → 73. The report now states `16 controls, 0 failed` at the top, so a reader can see the instrument was tested on the run that produced the numbers — a number whose provenance is not on screen is a number nobody can check.
  - **One known limit is recorded rather than papered over.** A gloss written in lowercase (`**PID** is the process ID`) is not recognised, because the `X is a Y` pattern requires a capitalised gloss specifically so it cannot accept a description — otherwise `The API is a black box` reads as a definition. `PID` therefore still appears in the actionable list. Widening the pattern for one term would start accepting descriptions, which is the wrong trade; the alternative is a `UNIVERSAL`-style allowlist entry, which is a different decision and was not taken.
  - **What is still open, and it is the same item as before:** the corpus report still needs a human to read it, and nothing fails the build when a domain term is genuinely never explained. What changed is that the report no longer lies about which terms those are.

## D-027 — Inline code is masked before emphasis, because the tokenizer was matching the wrong thing

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** `learning-site/src/lib/renderInline.jsx` is the site's whole inline-Markdown story: it turns `**bold**`, `*italic*` and `` `code` `` into React elements, because the curriculum's generated JSON carries that syntax through as plain text. It matched all three in **one alternation** over the string — `/(\*\*[^*]+\*\*|\*[^*\n]+\*|`[^`\n]+`)/g` — and recursed into a matched bold or italic run so that `` **`/etc`** `` still produced a `<code>` inside a `<strong>`. That worked for every string anyone had written, for months, because the bold branch's `[^*]+` content class is only ever violated by an asterisk, and an asterisk inside bold is rare.
  - Then `advance-04-cloud-identity-architecture` was written, and it contains this sentence: **The third statement uses `Resource: "*"` inside a key policy, and that is correct.** The code span's content is `Resource: "*"` — the asterisks are *data*, part of a JSON value, not emphasis.
  - **The bold branch could not match**, because `[^*]+` cannot span the asterisk inside the code span. The engine then tried the next alternative, `*italic*`, which is not anchored to anything — and it paired an asterisk from *inside* the code span with one from *outside* it. The result: the bold run was destroyed, the code span was destroyed, and the page rendered `*The third statement uses \`Resource: "*"\` inside…` with a visible backtick and no emphasis at all. `smoke-render.mjs` went red on that one lesson and passed on the other 28.
- **Decision:** **Paired code spans are extracted and masked before the emphasis pattern is ever applied.** `maskCodeSpans()` replaces every `` `…` `` run with a NUL-delimited index and returns the masked string plus the span texts; the emphasis alternation is then `/(\*\*[^*]+\*\*|\*[^*\n]+\*)/g` — **backticks removed from it entirely** — and `restore()` substitutes a `<code>` element back in at each leaf. A NUL cannot occur in the Markdown sources, so the sentinel is unambiguous. Recursion into bold and italic is unchanged, so `` **`/etc`** `` still nests correctly. The defect and the shipped sentence are pinned in a new unit test, `learning-site/scripts/test-render-inline.mjs` (34 cases, `npm run test:render-inline`, wired into `npm test`).
- **Consequences:**
  - **The old pattern was not failing to match — it was matching the wrong thing, and that is why the fix is structural rather than a wider regex.** The tempting patch is to widen the bold branch to allow asterisks (`\*\*[\s\S]+?\*\*`) or to require word boundaries around `*italic*`. The first makes bold swallow the rest of the line when an asterisk is unmatched; the second changes which italic spans render across the entire corpus, to fix a case that has nothing to do with italic. Masking removes the ambiguity at the source: **the emphasis branches only ever see markers that are genuinely markers**, which is a property no pattern tweak provides. This is the same reasoning as D-013's build-time index and D-022's machine-checkable class — fix the input to the decision, not the decision.
  - **The guard that caught it was written for this class and had never had a true positive.** `smoke-render.mjs` has asserted since D-011 that no literal `**` or backtick reaches rendered prose, excluding `<pre>` and `<code>` so a `markdown` fence can display `**bold**` as an example. It caught a real formatter bug on real curriculum content, on the first lesson that happened to write a quoted asterisk inside a code span. The assertion is worth more than it cost.
  - **A corpus assertion is not a rule assertion, and both now exist.** The smoke test can only catch this on content that already contains the shape, so the *next* lesson to write `"*"` inside a code span would ship the bug again and be caught only by a full smoke run — and only if someone ran it. `test-render-inline.mjs` holds the rule directly: the exact shipped string, plus code-containing-`**`, bold-containing-code, italic-containing-code, two code spans in one bold run, and unmatched markers left literal. It runs in seconds and needs no corpus.
  - **Three of the new test's own assertions were wrong first, and the third is the interesting one.** Two were harness bugs — I compared against React's HTML-escaped output without decoding `&quot;`, so a correct render "failed". The third was a false expectation of mine: I asserted `2 * 3 * 4` should render literally, but CommonMark genuinely pairs those asterisks and marks `" 3 "` italic, so **the formatter was right and the test was wrong**. It now asserts that behaviour and says why, so the next reader does not "fix" the formatter into disagreeing with Markdown. A test that encodes the author's assumption rather than the specification is worse than no test.
  - **The corpus audit that followed is the part to keep.** After the fix, the same diagnostic run over all 304 blocks of `advance-04` and every phase in all three tracks reported **0 blocks leaking a literal backtick into prose**, with 49 blocks legitimately containing backticks in their source. The bug was one string; the check is now the whole corpus.

## D-026 — A third track is a plumbing change, and six scripts had to be told about it

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** The curriculum had two tracks and **six scripts encoded that assumption**, each in its own list rather than in one shared registry: `build-content.mjs` (`byTrack = { it: [], cyber: [] }`, with an unrecognised track a hard failure), `audit-content.mjs`, `audit-refs.mjs`, `audit-readability.mjs`, `audit-terms.mjs` and `audit-time-budget.mjs`. The site had a seventh: `src/data/roadmaps.js` imported two JSON files and exported two tracks, which the sidebar, dashboard and search all read. A new track is **invisible** to every one of those, and the failure mode is the one this repository has now recorded four times in four different shapes: **a clean, confident report produced by a script that never read the new files.** The first attempt to widen them crashed instead, which was the *better* outcome — `readdirSync` on a directory that does not exist yet throws `ENOENT`, so the guards announced the problem rather than passing quietly.
- **Decision:** The third track is `advance-roadmap/` with track id `advance`, and every list above names it. A track is declared in those lists **before** its first phase file is written, so every walker tolerates a missing directory and **prints a note** naming the track it skipped. `add-frontmatter.mjs` replaces its string-prefix test (`it-roadmap/` meant `it`, everything else meant `cyber`) with an explicit `TRACK_BY_DIR` map that **throws** on an unmapped directory.
- **Consequences:**
  - **The guards are expected to be red while the track is half-built, and that is the proof they work.** With the overview written and the six phase files not yet, `audit-refs.mjs` reports 23 `PHASE_DOES_NOT_EXIST` findings (the overview genuinely references six phases that do not exist) and `audit-time-budget.mjs` reports 6 findings (six week counts with nothing to cross-check). Neither is a false positive; both are the checks refusing to certify a claim the repository cannot yet support. **The correct response is to finish the track, not to weaken the checks** — and the temptation to add an "ignore missing phases" exemption here is exactly how a gate acquires a permanent exception list.
  - **A silent skip is the one thing that was not allowed.** The tempting fix for `ENOENT` is `if (!exists) continue`, which produces `0 findings` on a track that was never read — indistinguishable from a clean pass. Every walker instead prints `note: <track> — no directory yet, skipped`, so the count in the summary can never be mistaken for full coverage. This is D-022 applied to a new case: the check is a claim about the text agreeing with itself, so a track that could not be read must say so out loud.
  - **`add-frontmatter.mjs` throwing is a deliberate change from guessing.** Ids are permanent and a reader's progress is stored against them, so a wrong id is worse than a missing one — a file tagged `cyber` that belongs to `advance` would silently merge two tracks' progress. The map now fails loudly instead of defaulting.
  - **The site side had one wrong label, not a crash.** `Search.jsx` read `hit.k === "cyber" ? "Cyber" : "IT"`, which labelled every non-cyber result IT — true only while there were exactly two tracks. It now resolves the label from the track registry. That class of defect is invisible in a smoke test that renders markup and never checks which words appear.
  - **The track is deliberately gated on prior employment.** The overview states plainly that it is not for someone who has not held a security role, and that all six phases are not meant to be taken. This is the same honesty the entry-level tracks apply to themselves, and it is the reason the overview says "take two or three" rather than listing six phases as a completion target.
  - **Current state:** complete. All six phase files and the track checklist are written, `audit-refs` and `audit-time-budget` are back to **0 findings** across 29 phases, and the red period lasted exactly as long as the track was half-built — which is the behaviour this record predicted, not a problem it describes. The track adds **74,547 lesson words across 6 phases** and 69 practice tasks, bringing the repository to 354,559 lesson words, 29 phases, 346 checklist task IDs and 252 practice tasks.

## D-025 — The time-budget guard gates, because the arithmetic class keeps coming back

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** The second comprehension pass found the arithmetic class productive for the third time. Across it: cyber 09 stated 60–80 hours above parts summing to 40–56, so the stated minimum exceeded the table's own maximum; cyber 02 stated 40–60 above parts summing to 37–59; cyber 02 called itself "the heaviest phase in the cyber track" while cyber 06 budgets 120–180 hours; the cyber overview listed phases 9–14 as 4 weeks each while every one of those phase files says 6 weeks in both frontmatter and prose; and IT 03 said "the scheduled four weeks at 5–8 hours a week" for a total of 20–30, where 4 × 8 = 32. `scripts/audit-time-budget.mjs` was written during that pass and ran clean, but it was never added to CI and never mentioned in `WORKFLOW.md`, `CHECKPOINT.md` or the commit checklist — so it was a guard nobody had to run and nothing would run for them.
- **Decision:** It runs in CI, in the content job beside `audit-refs.mjs`, and it is documented in `WORKFLOW.md` with its own checklist line. Four classes are checked, all arithmetic on the text rather than judgement: a budget table whose parts must sum to the total it prints, a stated week range whose minimum is above its maximum, frontmatter `duration`/`duration_weeks` disagreeing with the bold lead of the phase's own "Estimated time" line, and a track overview table listing a different week count than the phase file it points at.
- **Consequences:**
  - **It satisfies the rule D-022 stated for what belongs in CI.** A class belongs in a gate when it is a claim about the text agreeing with itself — does this number equal that number — and in a periodic human read when it is a claim about the reader's state. Every check here compares two places in the same repository, which is why it gates where `audit-terms.mjs` does not.
  - **The scoping is the load-bearing part, and it was got wrong first.** Class 1 keys off the table's own `Hours` column header, not off proximity to a heading. A schedule table (Weeks / study % / hands-on %) has time-ish cells but no Hours column and no total, so it is correctly out of scope; an earlier throwaway version summed bare "4-6" cells by accident and reported success on three tables out of the corpus. A table that *has* an Hours column but no checkable claim is a finding, not a silent skip, because a check whose skip path looks like its pass path is not a check.
  - **Class 4 had the same defect in a different shape, and it is recorded in the file.** The first version tracked "am I inside a table right now" rather than "have I seen a table row", and since the last table in a file is always followed by prose, the flag was false by the last line and the check never cross-checked a single week count. Both versions printed a clean result.
  - **Current state: `phases checked: 23`, `findings: 0`.** The defects above were fixed in the second pass; the guard is what keeps them fixed, since every one of them was introduced by an edit that changed one place and not the other.

## D-024 — The paragraph-density gate moved down to 110 words, and only after the backlog was zero

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** `scripts/audit-readability.mjs` gated on 150 words per paragraph. It had gated there since per-paragraph counts were added, and 150 was chosen deliberately high: at the time, **33 paragraphs sat over the 90-word editorial target** and a gate at 90 would have failed the build on the whole IT track. Those 33 were split at natural seams with every word preserved, and the backlog went to zero. What remained was four single paragraphs — IT 01 at 91, IT 03 at 95, IT 05 at 94, IT 07 at 93 — each a few words over the editorial line and none of them a wall of text. Those four were split in this pass. That left a gate at 150 enforcing a standard of 90: **the build could not fail on a paragraph a reader would actually find hard to read until it was two thirds longer than the point at which the writing had already been judged too dense.** A gate set 60 words above the standard it protects is not a guard; it is a formality that prints its own number and never acts on it.
- **Decision:** The gate is now **110 words** — `CEILING = 110` in `scripts/audit-readability.mjs`, with the editorial target named beside it as `EDITORIAL = 90`. 90 is reported every run and never fails; 110 fails. Both numbers live in one place rather than being spelled out in five strings, so the column headings, the summary lines and the failure message all follow the constant instead of drifting from it.
- **Consequences:**
  - **110, not 90, and the gap is the whole decision.** A gate at the editorial target would fail on every clarification a writer adds — the four paragraphs split in this pass exist because the text got *easier* to follow, not looser. 110 sits above that noise and below the point where a paragraph is genuinely a wall: it fails on prose a reader has to re-read, and passes prose that merely wants splitting. The failure that keeps a gate honest is one it can pass.
  - **The order matters, and it is why this is a decision rather than a one-line change.** The backlog was cleared first, then the gate moved. Lowering it while four paragraphs were still over 90 would have meant either grandfathering them — which is how a gate acquires a permanent exception list — or failing the build on known, already-scheduled work.
  - **The four splits preserved every word.** Each was a single paragraph carrying two ideas joined at a sentence boundary that already existed; the fix moved the break and changed no text. The audit now reports `Paragraphs over 90 words: 0 across 0 phases` and `Paragraphs over 110 words: 0`.
  - **The gate is provably able to fail and has not fired.** `0 of 23` phases outside target, longest paragraph **90 words (IT) / 86 (cyber)**. The honest note is the one this project keeps making: a green result on a guard is only good news once the guard is known to be capable of red, and the negative test here is a one-line change to `CEILING` rather than a defect anyone has actually introduced.

## D-023 — Six views are verified in a real browser, with no test dependency

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** Six things on this site — the notes panel, the dashboard rail, the print stylesheet, the Shared view, the Your work view and the time-budget control — had **never been rendered by a browser engine**. They were verified structurally (181 render smoke tests that assert on markup strings), by unit suites, and in one case by reading the CSS by hand. `test:smoke` renders to a string: it cannot see layout, computed CSS, focus order, a media query, or a control that is present in the DOM but unreachable. Both defects this project had previously found in that class were found by a human reading CSS, which does not scale and does not repeat.
- **Decision:** [`learning-site/scripts/browser-check.mjs`](../learning-site/scripts/browser-check.mjs) drives a real engine over the DevTools Protocol and asserts what a string render cannot. It is `npm run test:browser` and it runs in CI.
- **Consequences:**
  - **Zero new dependencies, and that was the binding constraint.** Puppeteer or Playwright would be a few hundred megabytes of browser download for what amounts to twenty CDP commands. Node 24 ships a global `WebSocket` and the protocol is JSON over one socket, so the whole driver is one file using `node:child_process`, `node:fs` and `fetch`. Rule 3 keeps build tooling dependency-free, and a test-only dependency is still a dependency.
  - **The assertions were written against real selectors read out of the source, not guessed.** `NotesPanel.jsx`, `TimeBudgetSelector.jsx`, `Shared.jsx`, `Dashboard.jsx` and the `@media print` block were each read first, so a selector that does not exist fails loudly rather than passing by finding nothing. That is the failure mode this whole decision exists to avoid: a check that asserts nothing is worse than no check, because it reads as green.
  - **The two highest-value assertions are ones no unit test can make.** First, **focus survival**: the driver types one character, asserts the textarea still has focus, types a second, and asserts the value accumulated. A parent that recreates the panel on each keystroke remounts the textarea and the reader loses focus after every letter — a real, common React defect, and invisible to any test that sets `.value` once. Second, **the print media query actually applying**: `Emulation.setEmulatedMedia({media:'print'})` and asserting the sidebar computes to `display: none`, the card borders to `none`, `--bg` to `#ffffff` and `break-inside` to `avoid` on code. The print path had only ever been checked as CSS text.
  - **Both writes are verified through the real storage key.** The budget control is clicked and `cs-roadmap:time-budget:v1` is read back; the note is typed and `cs-roadmap:notes:v1` is read back. A control that renders correctly but never writes is exactly the defect structural tests miss.
  - **The no-shame rule is now asserted in a live DOM, not just in a unit test.** `Your work` is checked for the absence of an `N of M` pattern, a percentage, and a `role="progressbar"` — the same guarantee `smoke-render.mjs` makes on markup, re-made on a rendered page.
  - **The honest result: it found no site defects, and that is recorded rather than celebrated.** Its first run reported three failures, and all three were the driver's own fault — it measured anchors on whichever Shared document opens first, while the resources list is the **third** (`shared.json` order is anti-burnout, tracker, resources). The fix selects the chip by name. "The browser check found nothing" is only good news if the check is known to be capable of failing, so the false start is written into the file's comment and into this decision.
  - **It runs in CI, where a browser genuinely exists — and that was verified, not assumed.** `ubuntu-latest` has Chrome at `/usr/bin/google-chrome`, so this is a standing guard and not a ritual someone has to remember. CI run **#43** lists `Browser check (real engine)` as a passing step in the site job, alongside the new `Audit cross-references` step in the content job. **CI run #45 is the stronger evidence**: it is the first run of the strict-mode version, and the step passed — which means a browser was found, because strict mode turns that into a hard failure. A green tick on that run is therefore a claim that 50 checks ran, not a claim that a skip was tolerated. `BROWSER_NO_SANDBOX=1` is set there and only there: a container does not grant the kernel capability Chrome's sandbox requires, and locally the sandbox stays on. The step starts `vite preview`, waits for it to answer with `curl` rather than sleeping a fixed interval, and kills it on both the success and failure paths — a background process started in an earlier CI step is not reliably still listening in a later one.
  - **A skip is honest locally and fatal in CI, which is a correction to this decision's first version.** It originally exited 0 whenever no browser was found, on the belief that `ubuntu-latest` had none. That belief was wrong, and the unconditional exit 0 introduced a worse problem than the one it solved: `completed / success` cannot distinguish `50 checks, 0 failed` from `BROWSER CHECK SKIPPED`, and this project has already been bitten three times by a guard that printed nothing and was read as green. So there are now two failure modes that a green tick would otherwise hide, and both are guarded:
    - **No browser under `BROWSER_CHECK_STRICT=1`** (set in the workflow) is a failure, not a skip. Verified by running it with `BROWSER_PATH` pointed at a nonexistent file: exit 1 with `BROWSER CHECK FAILED`. Without the variable the same conditions exit 0 with `SKIPPED`, which is the correct behaviour for a contributor's machine.
    - **`MIN_CHECKS = 40`** catches the other silent pass: a run that connected to an error page and executed almost no assertions. Assertions that never ran would otherwise look like assertions that held.
    - `BROWSER_PATH` exists so the skip path is testable at all. Without it the only way to exercise it was to break `PATH`, which also hides `node` and tests nothing — which is exactly what the first attempt at verifying this did.
  - **50 checks, 0 failed.** Rail geometry (320px, sticky, 4 cards, no horizontal overflow at 900px); budget (3 options, never `ongoing`, one checked, estimate wording, persists on click); notes (collapsed by default, no textarea until opened, focus survives two keystrokes, persists); print (sidebar and topbar hidden, borders and padding stripped, tokens repainted, code blocks unbroken, restored on return to screen); Shared (42 labelled anchors in 6 groups, `target="_blank"`, `rel="noreferrer"`, switching documents changes the body); Your work (renders the note written in the same session, no denominator of any kind); and no uncaught exception or console error anywhere in the run.

## D-022 — One class of comprehension defect is machine-checkable, and only one

- **Date:** 2026-09-16
- **Status:** Accepted
- **Context:** A comprehension audit of all 23 phases asked a question no existing guard could: *where does a motivated beginner with no IT background stop reading, and why?* It found four structural classes — a demonstration that silently fails (IT 05's Deny that does not deny), a worked artefact impossible as printed (cyber 05's weighted totals, cyber 06's priority formula, cyber 14's CVSS), a cross-reference to a section that does not exist or says the opposite (IT 06 → "Part 5's structure", IT 07 → a phantom "Part 8", cyber 03 → a Phase 1 that never defined least privilege), and a contradiction inside one phase (IT 09's two application targets and three follow-up intervals). Goal 3 required a mechanical guard for whatever class the audit **proves** detectable — and an audit that proves *nothing* is detectable is a result too, provided it is reported honestly rather than dressed up as a near miss.
- **Decision:** Two classes gate, and they are both arithmetic on the text: **`PART_DOES_NOT_EXIST`** — a `Part N` reference in a phase file where that file has no `Part N` heading, with `Phase N's Part M` resolved against phase N rather than against the containing file — and **`PHASE_DOES_NOT_EXIST`** — a `Phase N` reference to a number that track does not contain. They are implemented in [`scripts/audit-refs.mjs`](../scripts/audit-refs.mjs), exit 1 on a finding, and run in CI. A third class the script computes, **`FORWARD_AS_PRIOR`** (a phase citing a later phase as already-learned), is printed for a human and **does not gate**.
- **Consequences:**
  - **The guard found the IT 06 and IT 07 defects before they were fixed, and then found a real one nobody had reported.** On its first clean run against the fixed corpus it flagged `cybersec-roadmap/11-phase-incident-response.md:64` — "you reason about risk from Phase 13" in a phase numbered 11, in a sentence that opens "the skills from every phase before it converge". The cyber 10–12 audit reported the same line independently. Two checks reaching the same finding by different routes is the strongest evidence either produced.
  - **The guard was wrong twice before it was right, and both corrections are in the file rather than in a commit message.** Its first run reported `04-phase-hands-on-labs.md:763` — "Phase 3's Part 4, made concrete" — as a broken reference, because it resolved a phase-qualified reference against the containing file; `03-phase-security-fundamentals.md` does have a Part 4 (Web security), so the prose was right and the guard was wrong. Its second run, after that fix, flagged the *repaired* cyber 11 sentence, because the `PRIOR_CUE` regex included the bare word `from`, which matches "Risk reasoning **from** Phase 13 arrives later" — a clause that explicitly marks the phase as coming later, i.e. the exact opposite of the defect.
  - **`FORWARD_AS_PRIOR` is informational because a forward-reference judgement is not reliably mechanical — the same lesson the acronym scan taught.** Whether a sentence claims prior knowledge or disclaims it lives in verbs and clause structure, not in the string, and the guard has already been wrong about it once. `PRIOR_CUE` is kept tight (`defined`, `taught`, `covered in`, `gave you`, …) and the class is printed for a human. This mirrors [`docs/COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md) → "A negative result: the acronym audit does not converge", where `scripts/audit-terms.mjs` was demoted to a measurement that always exits 0 after five tuning passes left `AMD`, `USD`, `UTC` and `PID` in the residue — and `PID` was flagged on the line that defines it.
  - **The test for a guard, stated once:** a class belongs in CI when it is a claim about *the text agreeing with itself* — does this name exist, does this number exist. It belongs in a periodic human read when it is a claim about *the reader's state* — is this term decodable, is this sentence claiming prior knowledge. The first is cheap, exact and permanent; the second does not generalise, and shipping it as a gate produces a guard someone eventually disables.
  - **The audit itself is committed as a document, not as a script run.** [`docs/COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md) records the findings by phase, the two claims that were **rejected** as wrong after re-checking (IT 09's "wrong column" consequence — Part 4 and Exercise 1 both put Follow-up at position 7, so `G2` is correct), and the negative result. A clean bill for cyber 07 was returned and then checked rather than assumed: CVSS/CVE is taught at cyber 03, the setuid bit at cyber 02, Sysmon and Event IDs at cyber 03, the risk register at cyber 05.

## D-021 — Tasks carry a duration band, and the dashboard asks how long you have

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** The dashboard has always answered "What should I do today?" with the next unfinished checklist item that fits the reader's energy level. It could not answer a question a real beginner asks every day: *I have forty minutes. Is this a forty-minute task?* The curriculum's practice tasks ranged from "plug in a USB stick and watch Device Manager" to "install VirtualBox and build an Ubuntu VM" to "practise 20 technical interview questions across 8 topics", and nothing in the data distinguished them. A reader with half an hour was handed the VM. The obvious fix — a per-task minute count — was rejected only after research, not by assumption.
- **Decision:** Every practice task carries an authored duration **band**, not a number, in its metadata comment: `<!-- id: <PHASEID>-t<NN> band: <BAND> energy: <ENERGY> -->`. Four bands: `quick` (under 30 min), `focused` (30–90 min), `deep` (over 90 min), and `ongoing` (**not a single sitting** — recurring, week-gated, multi-session, or hardware-gated work). The dashboard asks how long the reader has, persists the answer as a preference, and offers a task whose band fits.
- **Consequences:**
  - **Bands, not minutes, because the underlying data is coarse and pretending otherwise would be a lie in the opposite direction.** 82% of the 183 practice tasks sit between 20 and 90 minutes, so any cut inside that range would be an arbitrary line drawn through a continuum — a 45-minute task would be "wrong" against a 44-minute estimate and no reader could tell. Only two honest edges exist: **under ~20 minutes** is essentially empty (4 tasks, 4%), and **over ~90 minutes** is *structurally* different rather than merely longer — multi-session projects, tool-curriculum cliffs, and writing volumes that cannot be finished in one sitting whatever the reader's pace. Phase-level durations (weeks, or "60–80 hours") were rejected as two to three orders of magnitude too coarse to schedule a Tuesday afternoon. Word count and verb choice were measured and carry almost no signal: two tasks both 27 words long differ sixfold in real time, and "Install VirtualBox" (90 min) sits in the same phrasing family as "Install CrystalDiskInfo" (20 min).
  - **Every band is an authored estimate, and the UI says so on the page.** Nobody has timed these tasks. Presenting a guess with the visual confidence of a measurement is the failure this label exists to prevent — a reader given a "45 minute" task that is really a three-hour install concludes the curriculum is unrealistic, not that the number was invented. The time control carries a visible note to that effect, and the smoke test asserts the word *estimate* survives on the rendered page.
  - **`ongoing` is an exclusion path, not a long duration.** More time does not turn a weekly retake into a sitting, and offering one as "today's task" would set the reader up to fail at it. `fitsBand` therefore fails closed on `ongoing` in both directions — it is never offered, and it is never treated as fitting.
  - **A real defect, caught by the new test rather than by a reader.** The first `pickToday` collapsed three different reasons into one "does not fit": a task needing longer, an `ongoing` commitment, and a task with no band at all. An unaddressed `ongoing` task left `smallestBlocking` null and fell through to `all-addressed`, so the dashboard would have told a reader **"nothing left" while they still had work** — just not work that fits in one sitting. That is the one sentence this page must never get wrong. The taxonomy was split instead of the test being relaxed: `only-ongoing` and `unjudged` now exist as distinct reasons with their own honest messages, and `test-today.mjs` asserts that leftover `ongoing` work never renders as everything being done.
  - **The picker walks curriculum order and never reorders.** It does not prefer larger tasks, smaller tasks, or the reader's favourites, and it does not shuffle. The suggestion is the first unaddressed task whose band fits, which keeps "what should I do today" answerable and stable across reloads.
  - **"Addressed" reuses what already exists and introduces no second progress system.** A task counts as addressed under exactly two rules: it has a non-blank answer in the notes store (D-019), or it belongs to a phase whose checklist is fully ticked. A phase with an *empty* checklist is guarded against — it must not read as complete, or a malformed phase would silently swallow its whole task list.
  - **The suggestion is drawn from practice tasks, and progress is still counted from the checklist.** The two lists are different on purpose: a checklist item is a claim about oneself ("I can use 20 basic Linux commands") and a practice task is an instruction ("Run `ss -tulpn` and identify listening services"). Only the second has a duration. The dashboard is the one place they meet, and the progress ring it shows is unchanged.
  - **`allPracticeTasks(track)` in [`data/roadmaps.js`](../learning-site/src/data/roadmaps.js)** is the read side, and [`lib/today.js`](../learning-site/src/lib/today.js) is pure — no React, no DOM — so the whole picker is exercised under plain Node in [`scripts/test-today.mjs`](../learning-site/scripts/test-today.mjs), 77 checks across fitting in both directions, the addressed rules, ordering, and every reason.
  - **The time budget is a tenth `localStorage` key** (`cs-roadmap:time-budget:v1`), registered in `transfer.js` → `KEYS` so a backup carries it, and owned by [`hooks/useTimeBudget.js`](../learning-site/src/hooks/useTimeBudget.js). It is a *preference*, like energy mode, so a merge keeps this machine's value rather than the incoming one. Its default is `focused`, because that is where 82% of the tasks actually sit and so is the least wrong guess for a reader who has never touched the control.
  - **`ongoing` is not offered as a time budget.** It is a property a task can have, not an amount of time a person can have, and offering it would let a reader select a budget the picker fails closed on — a control that appears to do nothing.

## D-020 — The shared documents are reachable, and the reader's writing is readable back

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** Two halves of the same problem. (1) `career-roadmaps/shared/` — the anti-burnout rules, the 42-resource list, and the weekly tracker template — has always been part of the curriculum and had **no path to it from the site**, because `build-content.mjs` walks for `*-phase-*.md` and these are not phase files. The reader those documents were written for, someone on a 34–112 week plan at risk of burning out, was the one least likely to ever see them. (2) D-019 shipped the writing side and deliberately no reading side: a note was reachable only by navigating to the phase that owned it. Writing that can only be found by remembering where you put it is a silo, not a workspace.
- **Decision:** Two views. **Shared** renders the three shared documents from a new build artifact, `shared.json`. **Your work** gathers every phase note and every per-task answer from all 23 phases into one page, with a link back to the phase that owns each one. Neither view counts what it shows.
- **Consequences:**
  - **One parser, two corpora, via a `headingBase` parameter.** Lessons are authored inside a `## Lesson:` wrapper, so their headings start at level 3; a standalone shared document is authored `# Title` / `## Section`, so it starts at level 1. Rather than fork the parser, [`scripts/lesson-ast.mjs`](../scripts/lesson-ast.mjs) takes `headingBase` and [`scripts/shared-content.mjs`](../scripts/shared-content.mjs) passes 1. The build **fails** on an unparsed block in a shared document, so a document that drifts out of the expected shape stops the build rather than shipping half-rendered.
  - **The Shared view deliberately does not reuse `<Lesson>`.** Lesson carries a table of contents with scroll-spy, per-section done controls, and a reading-position bar — all of it keyed by phase id and persisted through `useLessonProgress`. A shared document has no phase, no progress, and nothing to tick, so borrowing that machinery would mean inventing a fake phase id to hang state on. The blocks render directly, which is why there is a small local TOC rather than a reused one.
  - **The resource list renders as real anchors, from structured data.** `renderInline` deliberately does not autolink, so rendering the list as prose would produce 42 unclickable URLs — the document would look complete and be useless. `shared-content.mjs` parses `Name — https://url` lines and **fails the build** if the separator is wrong or a bullet appears before any category heading, because a silently mis-parsed catalogue is worse than a stopped build. The smoke test then asserts every single resource is an `href` and that no bare URL is left as text.
  - **Your work carries no denominator, and that is the point.** The summary says how many phases hold writing — a statement about *where things are*, with no "of 23", no percentage, no progress bar, and no ordering by volume. `summariseWork()` returns `{ phases, notes, answers, orphaned }` and deliberately has no `total`, `percent`, or `remaining` field, so the absence is structural rather than a rendering choice someone can undo by accident. A reader who wrote one line sees the same page a reader with 40 answers sees. This follows the same rule D-019 established for the writing side: a workspace that starts keeping score is a report card.
  - **Answers are matched by id and orphans are kept, not dropped.** An answer whose task no longer exists in the corpus is still the reader's writing; deleting it because the curriculum moved would be destroying the one thing on this site that cannot be regenerated. It is kept and flagged `orphaned: true`, and the page explains why it is there rather than presenting it as a normal answer.
  - **Your work reads via `readNotes()`, not `useNotes()`.** The hook owns the key and writes to it; a page that only displays is a second live copy of one `localStorage` key, and two live copies drift. This is the same discipline the dashboard rail follows with `readPortfolio()`.
  - **Both views are covered by the render smoke test, which found a real gap in the process.** A page whose entire content sits behind a click is invisible to server rendering — `Shared` holds the selected document in `useState`, so every render showed the first document and the resources list could not be covered at all. `Shared` gained an optional `initialId` prop, read only as the `useState` seed, so each document can be rendered directly. That is also the hook a future deep-link would need.
  - **Bundle cost: 20 KB for `shared.json`**, no new dependency. The two views are a page each plus one pure module.

## D-019 — The reader gets somewhere to write, and it is not scored

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** The curriculum constantly asks the reader to produce something. Nine phases were rewritten specifically so they end in an artefact the reader keeps — a baseline, a ticket log, an incident report, an interview story, a risk register. The site's entire support for that was **a checkbox**. A reader halfway through a 15,000-word lesson, looking at an exercise that says *write down what you noticed*, had nowhere in the site to write it: their notes lived in another app or nowhere, and the connection between the writing and the phase was lost. Worse, the tool that tracked their progress was the only thing that acknowledged the work, which taught the reader that the only work that counted was the work that could be ticked.
- **Decision:** A second local store — `cs-roadmap:notes:v1`, owned by [`hooks/useNotes.js`](../learning-site/src/hooks/useNotes.js) — holding one free-text note per phase and one answer per practice task. It is reached from the phase page: a **Your notes** panel, collapsed by default, and a **Write your answer** control on each practice task. Nothing in it is scored, counted, or reported anywhere else in the site.
- **Consequences:**
  - **It is a workspace, not a scoreboard, and that is enforced by a test.** There is no completion count, no "3 of 7 answered", no percentage, and nothing here feeds the dashboard. A reader who writes one line and one who writes nothing look identical to every other part of the site. `smoke-render.mjs` asserts that the rendered output of both new components contains no `N of M` or `N/M` pattern — a workspace that starts keeping score is the failure mode this decision exists to prevent, and it is the one an innocent later change is most likely to introduce.
  - **Practice tasks gained stable ids, which they never had.** They were emitted as plain numbered strings, so an answer keyed to a task had only its array position to go on — and inserting a task above it would silently move one reader's writing onto a different question. `scripts/build-content.mjs` now mints `<phase-id>-tNN` per task on the same principle the checklist already follows. **Honest limitation, recorded rather than hidden:** these ids come from position at build time, not from an authored `<!-- id: … -->` comment, so *appending* a task is safe and *reordering* one is not. The checklist solved this years ago by authoring ids in the Markdown; tasks have only ever been appended to, which is why it has not been needed yet. Migrating is a one-line change in that function when it is.
  - **The note and the answers merge differently, because they are different things.** A note is prose, and no machine can honestly choose between two paragraphs, so on import the existing note wins and an incoming draft is left alone. Answers are a map of id to text and union per task, existing winning on a collision. A phase that exists only in the incoming file is taken whole — that is the entire point of moving your work to a new laptop. All of this lives in `mergeValue` and is asserted in `scripts/test-notes.mjs`.
  - **An empty entry is pruned on write.** A phase the reader opened and typed nothing into does not appear in the store, and a note they cleared is removed rather than kept as an empty string. That keeps the backup file an honest description of what the reader has.
  - **It registered as the ninth key in `transfer.js` on the day it was written**, which is what that list was built for. Adding a key without registering it is exactly the silent omission `test-data.mjs`'s registry assertion exists to catch — and it did catch the follow-on: that suite hardcoded "eight keys" in five places and seeded only eight, so the ninth threw an unguarded `JSON.parse(undefined)` that took the whole file down with a stack trace naming neither the key nor the reason. The suite now reports a missing fixture value as a failed check instead of crashing, and the count assertions read nine.
  - **The panel and the answer boxes do not print.** D-017 exists so a phase can be taken to a train and annotated with a pen; a page already full of the reader's own earlier answers is a page with no room to write on. The tasks themselves still print, with blank space after each one.
  - **Bundle cost: 13 KB raw / 2.6 KB gzipped**, no new dependency. The store is a hook and two components; there is no library behind it.

## D-018 — In-lesson find is a second engine, not a second use of the first

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** Search (D-013) answers "where in the curriculum is this?" — a question asked *before* committing to a lesson. Once a reader is 15,000 words into one phase, the word they want is usually a few hundred words below them on the same page, and the corpus-wide box is a worse answer than scrolling: it switches views, and each result row loads its own lesson just to build a snippet. Browser find (Ctrl+F) is scoped correctly but cannot rank — it walks hits in document order, shows no context, and offers no jump-to-section.
- **Decision:** A second, smaller engine — [`lib/lessonSearch.js`](../learning-site/src/lib/lessonSearch.js) — that searches the block AST already in memory for the open lesson. It mirrors `useSearch.js`'s query semantics deliberately (same token regex, same trailing-punctuation strip, same exact-vs-loose tiers, same prefix and stem rules, same rarest-first intersection, same exact bonus) so the two boxes never disagree about what the reader typed. `components/LessonFinder.jsx` is the UI, collapsed by default behind a toolbar button because a reading page should not spend vertical space on a control used rarely.
- **Consequences:**
  - **Two deliberate divergences from the sibling engine, both because the corpus is one lesson instead of 23.** (1) `STOPWORDS` is a hard-coded grammatical list rather than a measured common set — a single lesson has no index to measure against. It deliberately does **not** include content words like `user` or `data`, which still discriminate inside one lesson; the corpus engine flags those, and copying that list here would widen a two-word query into a useless one. (2) An unresolvable term returns zero hits rather than tolerating it: across 23 lessons a missing term may be an indexing gap, but in one lesson it is simply absent, and the global box is the right tool.
  - **Section attribution is the nearest preceding h3/h4, and a heading owns itself.** A literal reading of "preceding" would attribute a hit inside `### Subnet masks` to the section above it, so a hit there would offer to jump to the wrong heading. h5 starts no section because `LessonBlock.jsx` renders it as a label with no TOC entry — attributing hits to a heading the reader cannot navigate to is worse than attributing them to the enclosing section. Blocks before the first heading get `sectionId: null` and are still indexed: the preamble is searchable prose, it just has no section to jump to.
  - **A snippet is three strings, not markup and not sentinels.** `{ before, match, after }` renders as three text nodes with the middle one emphasised, so the module never hands HTML to a caller that would have to trust it. Sentinel characters were rejected because a sentinel can legitimately occur in lesson text. `match` is the word as actually written, with sentence punctuation excluded.
  - **Every block type is indexed, including ones that do not exist yet.** Known types are handled explicitly; the `default` case walks any unrecognised block's string content with a depth cap, so a block that renders is searchable the day it appears. `buildEntries` emits exactly one entry per array position even for a malformed block, because skipping one would shift every later `blockIndex` and scroll the reader to the wrong place.
  - **`scripts/test-lesson-search.mjs` carries its own fixture** rather than reading `src/data/generated/lessons/`, which only exists after a build. It reproduces the shapes that have caused real bugs elsewhere — a sentence-final term carrying its punctuation, a nested bullet, an h5 that starts no section, a block before every heading — and asserts on section attribution, block-type reachability, ranking and snippet boundaries. The suite is in `npm test` as `test:lesson-search`.

## D-017 — A phase prints, because paper is the offline mode that already exists

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** The curriculum runs 34–112 weeks and the site is local-first by design (D-006), but "offline" had no answer for the case that matters most to a beginner: a train, a lab bench, a machine with no network, or simply a reader who wants to annotate a phase with a pen. None of that needs a service worker or a cache strategy, because the browser already has a mechanism for taking a page offline on paper, and the site had no `@media print` rules at all — a reader who hit Ctrl+P got the sidebar, the sticky toolbar, the reading bar and the dark theme.
- **Decision:** One `@media print` block at the end of [`styles/global.css`](../learning-site/src/styles/global.css), plus the finder styles above it. Everything that exists only to operate a browser is hidden — sidebar, off-canvas nav, topbar, reading bar, sticky lesson toolbar, in-lesson finder, per-section tick controls, code copy buttons, resume prompt, prev/next pager, and every modal or overlay. What prints is the lesson: prose, code, tables, the TOC, and the phase's structured sections.
- **Consequences:**
  - **The theme is repainted, not rewritten.** The site's own custom properties are overridden inside the print block (`--bg` to white, `--text` to black, and so on), so every component rule carries over instead of each one needing a print twin. The dark theme would empty a toner cartridge and is unreadable on a laser printout.
  - **Page-break rules are the substance of this decision, not the colour.** A code block split across a page boundary is unreadable and a table split mid-row is worse, so `break-inside: avoid` is applied to code, tables, blockquotes, list items and the table wrapper, and `break-after: avoid` keeps a heading with the text it introduces. A table that scrolls horizontally on screen has its wrapper set to `overflow: visible`, because a scrolled-away column on paper is simply lost.
  - **Link URLs are deliberately not expanded inline.** Appending the URL to every link would bury the prose in query strings; the curriculum's links are mostly documentation the reader looks up when back online.
  - **The reader's on-screen text size does not follow them onto paper.** The reading-size feature scales the lesson body through `--lesson-scale`, and a reader who set `A++` for a monitor would print a page with three paragraphs on it. Prose is pinned to 11pt and code and tables to 9pt.
  - **No dependency, no service worker, no cache.** The whole feature is CSS.

## D-016 — The reader's data is theirs to move, and an import is validated before it writes

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** Progress, portfolio, applications, schedule, reading position, section ticks and two preferences live in **eight** `localStorage` keys. The site is single-user and local-first by design (D-006), which is the right call — but it means a cleared browser profile, a new laptop, or a reinstall destroys months of work on a 34–112 week curriculum, and nothing in the UI said so. A reader could finish half a track and lose it to a routine "clear site data" without ever being warned.
- **Decision:** [`lib/transfer.js`](../learning-site/src/lib/transfer.js) exports all eight keys as one JSON document and reads that document back. `components/DataTransfer.jsx` is the panel, reached from the sidebar under **Data → Back up & restore**. Import is **validated before anything is written**, and a payload with one bad key is refused whole.
- **Consequences:**
  - **The module is pure, and that is what makes the rejection cases testable.** Storage is passed in as a two-method object rather than reached for as `window.localStorage`, so `scripts/test-data.mjs` exercises the entire round trip under plain Node — including the cases that matter, which are the failures. A backup format that silently accepts a malformed file and writes it over good data is worse than no backup at all.
  - **One bad key fails the whole import.** Half-restoring someone's progress and leaving the rest silently behind is the one outcome a backup must not have. Unknown keys are a different case and are *dropped and reported*, not refused: refusing the file over a key a newer version added would make forward compatibility impossible. A version mismatch is refused rather than half-read, for the same reason a failed validator is.
  - **Export omits rather than corrupts.** A key that is absent is not written at all; a key present but holding something this version cannot validate is omitted and listed in `skipped`, because exporting a corrupt value just moves the corruption to the file.
  - **Merge unions accomplishments and keeps this machine's position and preferences.** Progress, section ticks, portfolio and applications are things the reader made or finished, so losing one because another machine did not have it is exactly the failure this feature exists to prevent — they union. Reading position and the two preferences describe *this* device right now, and a merge that overwrote "where I am" with a stale value would yank the reader backwards on import. Existing wins on an id collision, so importing the same file twice is a no-op rather than a duplicate.
  - **Import is a modal, not a sidebar button next to "Reset progress".** It is the only action in the site that can overwrite the reader's work, and it does so across eight keys at once, so it shows the reader what the file contains key by key and applies nothing until they have seen that list. **Replace** exists for restoring onto a clean machine and is the second option, spelled out and confirmed.
  - **The page reloads after an import.** Every hook reads its key once at mount and then owns it in React state; an import writes `localStorage` underneath those live copies. Threading a refresh signal through eight hooks and getting it right in every one is more code and more ways to be partially correct than one reload after a settings errand.

## D-015 — The shell centres, and the dashboard gets a reference rail

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** With the reader affordances in place (D-014), the dashboard's remaining defect was visible before anything was read: on a 1,919px viewport it filled the left half and left the right half empty. Two causes, and they were separate.
  1. `.content` was capped at `--content-wide` (1,100px) and **left-aligned** against the sidebar. Even a page that wanted the width could not use it, and the dead band was a property of the shell, not of any page.
  2. The dashboard itself was a single column of cards. Nothing in it was wrong, but nothing in it was wide either — and the figures a reader actually checks (how far in, how long left, what is ready) were either absent or buried in prose.
  This matters more here than on a typical site because the curriculum is 34–112 weeks long. A reader returns for years; the page that answers "what should I do today" is the page they see most, and it was the least informative one.
- **Decision:** Two changes.
  1. **The shell centres and widens.** `--content-max` (1,400px) replaces `--content-wide` as the outer cap, applied with `margin-inline: auto`. `--content-wide` remains the cap for the one element allowed past the reading measure (tables). Centring is right here and left-alignment was not: every page is a column of cards, so the fix belongs to the shell.
  2. **The dashboard becomes two columns above 1,180px** — content plus a sticky **reference rail** carrying the progress ring, the plan's dates, three read-only readiness counts, and the next two phases. Phase cards also tile two-up once there is width for two honest cards.
- **Consequences:** The rail is the part that needs a boundary, and it has one. It is a reference column, not a second dashboard: no charts, no streaks, no leaderboards, per the anti-patterns in [`DESIGN-SYSTEM.md`](DESIGN-SYSTEM.md). Its only interactive elements are links to the page that owns each figure, and every count is phrased as "you have N" rather than "you are missing N".
  - **A rail that owns state would be a second source of truth.** The plan's dates come from `readStarts()`, the counts from `readPortfolio()` and `readApplications()` — plain reads, deliberately **not** the hooks. Two live copies of one `localStorage` key drift, and the stale one writing back would drop a record the reader just added on the page that owns it. The dashboard remounts on every view change, so a read at mount is correct; the hooks keep their subscribing behaviour for the pages that mutate.
  - **`fmtWeeks` and `fmtDate` moved into [`lib/pace.js`](../learning-site/src/lib/pace.js).** The rail and the Schedule page report the same numbers, and two copies of a formatter round them two different ways. `fmtDate` parses and formats as UTC, because formatting a UTC midnight in local time renders the previous day anywhere west of Greenwich — which turns a planned finish date into a wrong one.
  - **The ring is a chart, so it obeys the rule charts obey here.** The percentage and the raw count are rendered as text beside it; the `svg` carries a label rather than being silent; and it animates nothing. A non-numeric or out-of-range value clamps instead of reaching the dash arithmetic as `NaN`, which would render as no arc at all, silently — both cases are now asserted.
  - **Nothing is hidden at a narrow width.** Below the breakpoint the rail lays out as cards under the main column, in the same order. This is a reflow, not a truncation, and the 560px phone layout is unchanged apart from the phase grid dropping to one column.
  - **The main bundle grew by 4 KB raw / 0.7 KB gzipped** — 398 KB and 117.7 KB — because the ring and the rail add code and no dependencies. The search index is untouched.

## D-014 — Progress has two axes, and a phase remembers where you were

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** The site renders each lesson body in full (D-011) and ships it as its own file loaded on demand (D-012). Measurement afterwards exposed the shape of a problem that had been invisible while the lessons were only on GitHub: a lesson is **84–95% of its phase file**, IT Phase 1 alone has 66 `###`/`####` subheadings, and the longest runs ~15,500 words. Against that, the only completion signal in the application was the phase checklist — 7–19 items sitting **below** the lesson. A reader four hours into a lesson had no way to record that they had finished the section they were reading, no sense of how far in they were, and no way back to where they stopped. Meanwhile the phase page ended after its exit criteria with **nothing after it**: on a 9–14 phase track whose whole point is sequence, there was no "next" anywhere in the UI, and the only route to the following phase was to scroll to the top and aim at the sidebar.
- **Decision:** Three separate pieces of state, kept deliberately apart.
  1. **Lesson section state** ([`hooks/useLessonProgress.js`](../learning-site/src/hooks/useLessonProgress.js)), keyed by the heading id the lesson parser already emits and namespaced by phase id — `it-01#part-7`. Persisted under its own key, `cs-roadmap:lesson-sections:v1`.
  2. **Reading position** ([`hooks/useReadingState.js`](../learning-site/src/hooks/useReadingState.js)), recording the last phase opened and, per phase, the last heading on screen, under `cs-roadmap:reading:v1`.
  3. **Schedule start date** ([`hooks/useSchedule.js`](../learning-site/src/hooks/useSchedule.js)), one date per track, under `cs-roadmap:schedule:v1`.
  Navigation is derived, not stored: [`data/roadmaps.js`](../learning-site/src/data/roadmaps.js) gained `neighbours(track, phaseId)`, which reads the phase array's order rather than parsing titles.
- **Consequences:** The application gains a reason to distinguish *reading* from *completing*, and the distinction is enforced structurally rather than by convention — three storage keys, three labels, two counters on a phase page that never merge. This is the part worth recording, because the easy version of this feature is one progress bar, and one progress bar would make one of the two numbers lie: either section ticks would inflate the phase completion test the exit criteria depend on, or finishing a phase would silently discard the reader's place in a lesson.
  - **A heading id is the identity, and it derives from the heading text.** Rewording a heading retires its state rather than mis-attributing it — the same rule the checklist IDs follow ([`CONTENT-SCHEMA.md`](CONTENT-SCHEMA.md) → Checklist task IDs). A scroll offset was rejected for the same reason: an offset into content that can be regenerated is a number with no meaning after the next content edit.
  - **"Reset progress" does not clear reading state.** The button says "Reset all progress" and the reader means the checklist. Deleting their place in a lesson they were halfway through would be a surprise the label did not warn about.
  - **The resume prompt suggests and waits.** Nothing jumps on load. A reader who deliberately scrolled to the top has said something by doing so.
  - **The reading bar measures the lesson, not the page.** A document-scoped bar would count the checklist, tools, tasks, deliverable and exit criteria as reading, and would report a reader halfway through a lesson as a fraction of the whole page.
  - **A keyboard shortcut must not be able to change saved state.** Navigation, search and view switching are bound; checkbox toggling is not, and that omission is a decision rather than an oversight.
  - **`test-ui.mjs` was added because none of the existing guards could see any of this.** `smoke-render.mjs` runs no effects and no handlers, so it cannot observe a shortcut that never fires, a pace figure wrong by a week, or two phases colliding on a section key — and a collision would render a tick in one phase as a tick in another, which is invisible in every screenshot and every render assertion. The section-key namespacing has its own test for exactly that reason.

## D-013 — Search uses a build-time inverted index, not the lesson text

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** With 23 lessons rendering in the site, reaching a specific answer meant browsing — the sidebar gets you to a phase, not to the paragraph. Full-text search was the obvious next step, but the naive implementation is a trap here. The lesson JSON is 1.7 MB and is deliberately loaded one file at a time (D-012); an index that stored segment text was **measured** at 1,438 KB raw / **473 KB gzipped**, because it duplicated every lesson. An average lesson gzips to about 16 KB, so that index cost the reader roughly thirty lessons' worth of download for a feature used occasionally — the wrong trade for an audience on modest connections, which is exactly who this curriculum targets. A second attempt, windowing long segments with overlap to improve snippets, was also measured and made it **worse** (1,438 KB to 1,501 KB), because the overlap duplicated text.
- **Decision:** Emit an inverted index — term to segment id — and no prose. `scripts/search-index.mjs` builds it at build time into `generated/search.json`; `hooks/useSearch.js` fetches it once, lazily, on the reader's first search, and renders snippets from the lesson file the site already downloads when a phase is opened. Segments are cut at `###`/`####` headings, which lands a result on roughly a screenful and gives every hit a natural title. Postings are delta-encoded in base36 against document order. The index measures **471 KB raw / 180 KB as a delivered chunk**, and `index.html` does not reference it — the main bundle is unchanged at 105 KB gzipped, so a reader who never searches pays nothing.
- **Consequences:** The site gets search without undoing D-012. Three defects were found by testing the index against the query engine rather than by reading either one, and all three produced *silent* wrongness — a search box that returns nothing or the wrong thing without erroring:
  - **Pruned common terms.** Terms appearing in more than 20% of segments were originally *deleted* to save space. Since a query is an AND over its terms, one deleted term made the whole query return zero results: `"user account locked"` found nothing because `user` was gone. Common terms are now kept and flagged, so the engine matches them but does not let them narrow the result set, and the UI says which words it ignored.
  - **Punctuation glued into terms.** A sentence ending `"...was timestomped."` indexed the term with the period attached, so `timestomped.` and `timestomped` were different entries. Trailing separators are now stripped; interior punctuation is kept, because `get-adprincipalgroupmembership` and `page_fault_in_nonpaged_area` are exactly what a reader pastes in.
  - **Exact matches suppressed broader ones.** Searching `timestomping` matched only that exact term (modules 10 and 12) and never consulted the stem that would also have found module 11's `timestomped` — so typing *more precisely* returned *fewer* results. Exact and prefix matches are now collected separately and scored in tiers, with an exact-match bonus. Simply merging them was also wrong in the other direction: `STAR` exactly matches the interview method but also prefixes `start`, `startup` and `starved` (227 segments across all 23 lessons), and a merged set drowned the lesson that actually teaches STAR.
  - `learning-site/scripts/test-search.mjs` guards all three, plus ID range and ordering, empty and nonsense queries, and that every result points at a lesson file that exists. Like the lesson parser, a search bug does not crash the page — it just returns the wrong answer — so the guard has to assert on known-correct hits rather than on the absence of exceptions.

## D-012 — Lesson bodies are emitted per phase and loaded on demand

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** D-011 added the lesson body to the generated JSON. Measured afterwards, the lesson region is 84–95% of every phase file, and its JSON totals roughly 1.6 MB across both tracks. Because `src/data/roadmaps.js` imported the track files statically, Vite inlined all of it: the JS bundle went from 265 KB to 1.96 MB, so a reader who opened the dashboard downloaded the entire curriculum before seeing anything.
- **Decision:** Keep the track index files small (goal, skills, checklist, tools — about 200 KB total) and emit each lesson as its own file at `generated/lessons/<phase-id>.json`. The site loads one with a dynamic `import()` in `src/hooks/useLesson.js`, keyed by `phase.lessonPath` carried on the phase record, and caches per phase id in memory. The phase record also carries `lessonBlockCount` and `lessonHeadingCount` so the dashboard can describe a lesson without fetching it.
- **Consequences:** The initial bundle returns to ~340 KB and each lesson becomes a 30–170 KB chunk fetched when its phase is opened. This is the first split point in the app; a reader who never opens a phase never downloads a lesson. The cost is a loading state and an error state in `PhaseDetail`, and the requirement that `build-content.mjs` runs before the site is served — already true, since `dev` and `build` both call it. Vite needs a literal `import.meta.glob` pattern to discover the files, so the path is templated rather than computed.

## D-011 — The site renders the lesson body, not just the structured sections

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** `build-content.mjs` originally extracted goal, skills, topics, tools, tasks, checklist, and exit criteria — and dropped the `## Lesson:` region entirely. Measured per phase that region is 84–95% of the file, so the site was showing roughly a tenth of the curriculum: a checklist *about* a lesson rather than the lesson. The prose, the worked examples, and the tables were reachable only through a link to GitHub.
- **Decision:** Parse the lesson region into a block AST at build time with `scripts/lesson-ast.mjs`, and render it in the site with `components/Lesson.jsx` and `components/LessonBlock.jsx`, behind a table of contents built from the lesson's `###`/`####` headings. The Markdown stays the single source of truth; the AST is a build artifact like the rest of the JSON. No Markdown dependency is added — the parser supports the subset the curriculum actually uses, which was measured across all 18 phases before being written.
- **Consequences:** The site is now a reader for the curriculum rather than an index of it. Two guards protect the new path, because a parser bug does not crash anything — it just makes content vanish: `audit-lesson-ast.mjs` compares every lesson's AST against its source with markup stripped and fails on any loss or unrecognised construct, and `build-content.mjs` fails the build when the parser reports an unhandled construct. `smoke-render.mjs` renders the lesson for every phase and asserts that tables, code blocks, and headings all appear and that no literal `**` or backtick reaches rendered prose. Inline formatting needed one addition: the curriculum writes bold containing code (``**`/etc`**``), so `renderInline.jsx` now recurses into bold and italic runs instead of a single-pass alternation.

## D-010 — The sidebar is viewport-pinned, and becomes a drawer on small screens

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** `.app-shell` is a flex row and `.sidebar` had no height or position constraint, so the sidebar stretched to the full document height and scrolled away with the page. On a long phase the reader had to scroll back to the top to navigate. Below 720px the sidebar also stacked above the content, which pushed the page down and left the nav no closer.
- **Decision:** Give the sidebar `position: sticky; top: 0; height: 100vh; overflow-y: auto` so it stays pinned and scrolls independently. Below 860px, replace the stacked layout with an off-canvas drawer: fixed, translated off-screen, opened by a hamburger button in a sticky topbar, and closed by the backdrop, the close button, or Escape. Background scroll locks while it is open and the open phase is scrolled into view in the list.
- **Consequences:** The nav is reachable from any scroll position. The drawer adds one piece of state to `App.jsx` and needs Escape handling and scroll locking to avoid trapping the page behind an overlay — both are in place. View changes now reset scroll to the top, which also fixes opening a phase at the previous page's scroll offset.

## D-009 — Publishing the site to GitHub Pages, and the base path that makes it work

- **Date:** 2026-09-15
- **Status:** Accepted
- **Context:** D-008 permitted CI but ruled deployment out of scope and noted that "publishing the site would need a decision of its own." This is that decision. The site is a static Vite build, so it can be hosted anywhere; the constraint that shapes the rest is that GitHub Pages serves a *project* site from a subdirectory of a shared domain — `https://markkramm.github.io/cs-roadmap/` — rather than from a domain root.
- **Decision:** Publish the site to GitHub Pages from a dedicated `.github/workflows/deploy-pages.yml` workflow. Because the site is served from a subdirectory, `learning-site/vite.config.js` reads `process.env.VITE_BASE` and falls back to `/`, and the Pages build sets `VITE_BASE=/cs-roadmap/`.
- **Consequences:** Rule 3 in `AGENTS.md` gains a third bounded carve-out: `.github/workflows/` may now contain a deploy step, but only to GitHub Pages, and only for the built site. The content stays dependency-free — the deploy publishes generated output and adds nothing that reading `career-roadmaps/` depends on. Vite emits absolute asset URLs, so without the prefix the published page loads blank and every `/assets/...` request 404s; the workflow therefore *verifies* the emitted HTML and fails the build if the prefix is missing, rather than trusting the environment variable. Hard-coding the prefix was rejected because it would break `npm run dev`, `vite preview`, and the Netlify build, all of which serve from a root. Netlify is left working and unchanged as a fallback host.

## D-008 — CI is permitted, deploy is not

- **Date:** 2026-09-11
- **Status:** **Superseded by D-009** for the deploy question only; the CI carve-out still stands as written.
- **Context:** The roadmap's remaining Next item is CI — run `lint:content`, `build`, and `test:smoke` on every push. A GitHub Actions workflow lives at `.github/workflows/`, which is outside the `learning-site/` + `scripts/` scope that D-005 permits under rule 3 of `AGENTS.md`. The rule blocks the workflow file even though CI adds no runtime dependency to the content.
- **Decision:** Permit `.github/workflows/` **solely for continuous integration** — linting, building, and smoke-testing the repository on push and on pull request. No deploy step, no publishing, no secrets, no credentials. Deployment stays out of scope.
- **Consequences:** Rule 3 in `AGENTS.md` gains a second explicit, bounded carve-out. The dependency-free guarantee survives intact, because CI *checks* the content rather than becoming a dependency of reading it — `career-roadmaps/` and `docs/` are still readable with just `git` and a text editor. A future contributor knows CI is allowed and deployment is not; publishing the site would need a decision of its own. **That decision is D-009.**

## D-007 — View navigation is local state, not a router

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** M2 adds three destinations (tools library, portfolio tracker, application tracker) alongside the dashboard and the per-phase detail view. D-006 left routing open. The site is a single-user local tool: no server, no shareable URLs, no deep links, no analytics. A router would add a dependency and a URL surface to solve problems this tool does not have.
- **Decision:** Keep navigation in `App.jsx` as a single `view` string — `dashboard` | `phase` | `tools` | `portfolio` | `applications` — alongside the existing `trackId` and `openPhaseId`. No router dependency.
- **Consequences:** No new dependency. The browser's back and forward buttons do not move between views; the sidebar and the in-page Back button are the only navigation, which is why both stay visible on every view. If deep links are ever wanted, the switch in `App.jsx` is the single seam to replace — no component below it needs to change.
- **Amended by D-014:** the view list gained `schedule`, and keyboard shortcuts now reach every view — but navigation is still local state, and the browser's back button still does not move between views. D-014 added a second, complementary route through the content (`j`/`k` between phases, `g d` and `g s` between views) rather than a URL surface.

## D-006 — Learning site is built with React and Vite

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** Milestone M1 (see [`ROADMAP.md`](ROADMAP.md)) needs a UI with real interactive state: per-task checkboxes, progress computation, energy-mode filtering, and a daily-task recommendation. A hand-written DOM layer would grow quickly and slow iteration.
- **Decision:** Build the learning site with React and Vite under `learning-site/`, scoped by D-005. Plain CSS, no component library, no state-management library. Progress persists in `localStorage`. There is no backend, no auth, and no database.
- **Consequences:** `learning-site/` gains `package.json` and a build step. The study content and `docs/` stay dependency-free, so the rest of the repository is unaffected. Framework-specific choices (routing, icon set, animation) remain open.

## D-005 — Scoped build-tooling exception for the future learning site

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** Rule 3 in `AGENTS.md` forbids build tooling repo-wide. A personal learning site (see [`ROADMAP.md`](ROADMAP.md) → Learning Site M1) will need a bundler and a `package.json`.
- **Decision:** Permit build tooling **only** under `learning-site/` (and `scripts/` for content tooling). The rest of the repository stays build-free, so `career-roadmaps/` and `docs/` remain readable with just `git` and a text editor.
- **Consequences:** Rule 3 in `AGENTS.md` is amended with explicit scope. Future contributors know the exception is bounded.
- **Amended by:** D-008 (a second, check-only carve-out for `.github/workflows/`).

## D-004 — Add `.editorconfig` alongside `.gitattributes`

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** `.gitattributes` normalizes line endings at Git time, but files can still be written with CRLF on disk by the editor, causing repeated churn.
- **Decision:** Add `.editorconfig` so compliant editors save UTF-8/LF by default. Windows-native scripts keep CRLF.
- **Consequences:** Two layers of protection; contributors get correct behavior automatically.

## D-003 — Keep the repository build-free

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** The content is plain Markdown. A static-site generator was considered but would add dependencies and a toolchain.
- **Decision:** Ship content only. No `package.json`, no bundler. A site, if ever built, must be a separate opt-in.
- **Consequences:** Maximum portability and zero setup cost; less flashy presentation.
- **Amended by:** D-005 (build tooling scoped to `learning-site/` + `scripts/`) and D-008 (CI under `.github/workflows/`). The content itself stays build-free.

## D-002 — Standardize on UTF-8 (no BOM) with real typographic characters

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** A lossy UTF-8→ASCII conversion had replaced em-dashes, en-dashes, curly quotes, and box-drawing characters with literal `?`.
- **Decision:** Require UTF-8 without BOM and real typographic characters; document the mapping in [`WORKFLOW.md`](WORKFLOW.md).
- **Consequences:** Content reads correctly everywhere; contributors must avoid ASCII substitutes.

## D-001 — Normalize all text files to LF

- **Date:** 2026-09-11
- **Status:** Accepted
- **Context:** Mixed CRLF/LF caused whole-file diffs and obscured real changes.
- **Decision:** `* text=auto eol=lf` in `.gitattributes`; CRLF reserved for `.bat`, `.cmd`, and PowerShell scripts.
- **Consequences:** Clean, reviewable diffs.