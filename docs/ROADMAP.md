# Project Roadmap

The roadmap for the **repository itself** (for the study curriculum, see [`../career-roadmaps/`](../career-roadmaps/)).

## Done

- [x] Recover all 29 content files from Git objects after the working tree was emptied.
- [x] Fix encoding corruption (literal `?` where typographic characters belonged).
- [x] Normalize all text to LF via `.gitattributes`.
- [x] Add root `README.md` and `.gitignore`.
- [x] Add `.editorconfig` to enforce UTF-8/LF at save time.
- [x] Add `docs/` meta-documentation, `AGENTS.md`, and `CHANGELOG.md`.
- [x] Add `docs/CONTENT-GUIDE.md`, `docs/FAQ.md`, and `docs/TROUBLESHOOTING.md`.
- [x] Add site-prep specs: `docs/CONTENT-SCHEMA.md` and `docs/DESIGN-SYSTEM.md`.
- [x] Add root `CONTRIBUTING.md`.
- [x] Publish to a Git remote (GitHub) and push `main`.
- [x] Decide on a license — CC BY 4.0.
- [x] Learning Site Milestone M1 — scaffold `learning-site/`, build the content pipeline, add the app shell and dashboard.
- [x] Content linter — `scripts/lint-content.mjs` checks CRLF, BOM, U+FFFD, invalid UTF-8, and stray `?` substitutes.
- [x] Render smoke test — `learning-site/scripts/smoke-render.mjs` renders every page and every phase with real data, catching the class of bug a build cannot.

- [x] Learning Site Milestone M2 — view navigation (D-007), tools library, portfolio tracker, application tracker. Five commits; the render smoke test was extended in the same commit as each new page.
- [x] Tooling hardening — the linter scans `LICENSE`, and `build-content.mjs` fails loudly on a malformed resource line instead of emitting `url: null`.
- [x] CI — `.github/workflows/ci.yml` runs the three checks on every push to `main` and every pull request, as two jobs: content integrity (no install) and learning site (`npm ci`, build, smoke test). Permitted by D-008.
- [x] **Content-depth pilot** — `career-roadmaps/it-roadmap/01-phase-computer-fundamentals.md` deepened with a 4,430-word `## Lesson` section. Markdown-only: no pipeline or renderer change, no new dependency. Pilot judged successful.
- [x] **IT lesson writing, Phases 1–4** — `## Lesson` sections written for Phases 1–4 (4,430 / 4,337 / 3,370 / 6,181 words). Structural repairs made in the same pass:
  - Phase 1: duplicate `## Deliverable` and `## Checklist` removed; orphaned bullets folded into topics; redundant `## Resources` dropped; deprecated `wmic` replaced with `Get-PhysicalDisk`.
  - Phase 2: rebuilt from the Git original to restore the section contract.
  - Phases 3 and 4: lessons were mis-inserted between `## Goal` and `## Estimated time`, `## Estimated time` was duplicated, tasks were stranded under a non-contract `### Hands-on Tasks` heading, and lesson prose had leaked into `## Specific topics`. All fixed.
- [x] **Stranded-task recovery** — Phases 1, 3, and 4 were generating **zero** practice tasks in `it.json` because their tasks sat under `### Hands-on Tasks` rather than the contract heading `## Hands-on practice tasks`. Restoring the heading took the totals from 0/0/0 to 5/8/6. Structural diff against the pre-repair baseline: 75 IDs before and after, zero missing, zero extra, zero count changes.

- [x] **IT lesson writing, Phases 5–9** — `## Lesson` sections written for Phases 5–9 (3,039 / 3,126 / 3,343 / 3,214 / 3,757 words). **No structural repair was required:** all five phases already had clean section order and their Markdown task counts matched the extracted JSON exactly (7 / 6 / 6 / 7 / 7), so this was pure content work. Phases 8 and 9 have no `## Specific topics to learn`, so the lesson was placed after the last structural section (`## Resume sections` and `## PH-friendly job boards` respectively), keeping the order skills → structure → lesson → tools.
- [x] **IT track lesson pass complete** — all nine IT phases carry 3,000+ word `## Lesson` sections. Structural diff against the pre-edit baseline: `it.json` identical, 1,585 lines, zero diffs. A further diff against a scratch worktree at `HEAD` found a regression no earlier check could see: **Phase 4's tools table had lost two rows** (`Microsoft Teams`, `Google Workspace Admin Help`), which had been left stranded mid-lesson and had quietly cut the tools library from 112 tools to 110. Rows restored; tools back to 112. Tool rows carry no IDs, so ID-based comparison was blind to it — see [`WORKFLOW.md`](WORKFLOW.md).

- [x] **Committed the IT lesson pass** — Phases 1–9, the structural repairs, and the doc updates are in. Verified that `HEAD` builds in a scratch worktree and reproduces the working tree byte-for-byte apart from `generatedAt`.
- [x] **Cyber lesson pilot** — `cybersec-roadmap/01-phase-foundations.md` deepened with a 4,965-word `## Lesson` section, written to the IT standard: a "Why this lesson exists" opener, five `### Part N` sections, and closing "Key takeaways" / "Practice this next" that point back at the seven existing tasks. **No structural repair was required** — the phase already had clean section order. The generated JSON is byte-identical to `HEAD`, confirming the lesson is invisible to the pipeline as intended. Pilot judged successful; the format carries over.

- [x] **Cyber lesson, Phase 2** — `cybersec-roadmap/02-phase-networking-and-linux.md` deepened with a 6,841-word `## Lesson` section, the longest in the cyber track. It is the first cyber lesson to need five `### Part N` sections plus the opener, because the phase spans two subject domains (networking depth, then Linux depth) and closes with lab setup and the Bandit learning path. **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 3** — `cybersec-roadmap/03-phase-security-fundamentals.md` deepened with a 8,158-word `## Lesson` section, the longest in the repository. The phase spans six topic domains, so the lesson carries six `### Part N` sections (identity; endpoint and malware behaviour; network; web; vulnerability management; incident response) and closes by tying them together with a worked phishing incident timeline that demonstrates the phase's exit criterion. **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 4** — `cybersec-roadmap/04-phase-hands-on-labs.md` deepened with a 4,506-word `## Lesson` section. This phase carries the optional `## Lab setup options` section, so the lesson slots after it and before the tools table, per the placement rule. Three parts: designing the lab before building it (diagram, host-only isolation, static addressing, baselines), building it (install order, snapshots, what Wazuh is, the **source → decoder → rule → alert** pipeline with a real annotated custom rule), and generating evidence and writing it up (the generate-observe-adjust loop, incident report structure, and why blue-team labs simulate the actual job). **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 5** — `cybersec-roadmap/05-phase-specialization-choice.md` deepened with a 4,913-word `## Lesson` section. This phase carries the optional `## Path options` section, so the lesson slots after it and before the tools table — the second placement case confirmed. Uniquely among the cyber lessons, its job is to drive a **decision** rather than teach a subject, because the phase's exit criterion is a sentence the learner must be able to say: target role plus the three projects that prove it. Three parts: the four paths described honestly (work, day-to-day feel, best fit, entry reality, and entry barrier for each, plus a comparison table); how to decide (the four-axis scoring with the trap in each axis, reading 20 job posts methodically, matching the learner's specific background to a path, primary plus adjacent backup, and the five components of a 90-day plan); and making the choice stick (the switching trap and its mechanisms, what focus buys, when revisiting is legitimate, and how this phase feeds Phases 6–8). **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 6** — `cybersec-roadmap/06-phase-portfolio-projects.md` deepened with a 5,672-word `## Lesson` section. This phase carries the optional `## Required projects` section. Three parts: what a portfolio actually has to do (the recruiter's ninety seconds versus the hiring manager's ten minutes, the anatomy of a portfolio report, evidence that survives scrutiny, sanitising artifacts before publishing, and what "clear" means mechanically); building the three projects (the install order that confirms every pipeline layer early, the three Wazuh rules that demonstrate three different skills including the tuning story, analysing traffic as behaviour rather than a protocol tour, using a platform lab but going beyond its questions, and scoping every project with a written definition of done plus a week-by-week schedule); and packaging it (repository structure, the top-level README as the one guaranteed-read file, honesty as a competitive advantage, and the five interview questions to rehearse). **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 7** — `cybersec-roadmap/07-phase-certifications.md` deepened with an 8,929-word `## Lesson` section, now the longest in the repository. This phase carries the optional `## Recommended order` section. Five parts: what a certification actually does (the three jobs — filter, syllabus, follow-through — and equally what it does not do; the free-study/paid-exam split; and the "buying progress" trap); the certification landscape described honestly, with a ten-row comparison table, including that A+ is largely redundant for someone already in IT support, that ISC2 CC may be free through a workforce initiative, that Security+ is the strongest paid candidate, and that GRC and pentest credentials are not entry points; deciding with evidence (the three questions, the 30-job-post test separating Required from Preferred from Mentioned with a worked table, the green/yellow/red objective-mapping method with a worked example, the free-first rule, and the five-section decision document); studying for free (where the free material is, active recall and spaced repetition over passive video, a 12-week schedule holding hands-on work at 40%, and exam-day logistics for a Philippine connection and test-centre availability); and certifications on paper and in the room (honest resume representation, "studying X" versus "in progress", and three prepared interview answers). **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber lesson, Phase 8** — `cybersec-roadmap/08-phase-job-application.md` deepened with a 7,358-word `## Lesson` section. This phase carries the optional `## Target roles` section. Four parts: understanding the market (the application funnel as a table showing why 50+ applications is the honest baseline, what "entry-level" actually means — no prior *security* experience, not no prior work experience — what is genuinely hard versus genuinely easy, and a scam warning); building the artifacts that get you screened in (resume mechanics for both the software filter and the thirty-second human read, a truthful IT-to-security bullet translation table, presenting labs honestly and why that is the stronger move, and the tracker's ten columns with the keyword and notes columns doing the real work); the interview (what it actually tests, eight model technical answers with real commands and honest boundaries, the two-minute and ten-minute project explanations, STAR, and the four hard follow-up questions); and running the search without burning out (a weekly cadence table with energy levels, diagnosing failure by funnel stage, networking and referral sequencing, and holding course through silence). **No structural repair was required** and the generated JSON is byte-identical to `HEAD`.

- [x] **Cyber track lesson pass complete** — all eight cyber phases carry `## Lesson` sections of 4,510–8,929 words (4,969 / 6,848 / 8,162 / 4,510 / 4,919 / 5,672 / 8,929 / 7,358). Combined with the IT track's nine, every phase in the repository now carries full lesson prose.

- [x] **Phase 1 structural repair and depth pass** — `it-roadmap/01-phase-computer-fundamentals.md` had four defects that rendered it visibly broken, all predating the build-parser fence fix:
  - **A missing blank line** glued `### Part 2 — RAM` to the preceding paragraph, so the Part 2 heading did not render at all.
  - **Part 2's `#### What you would do` was empty**, and its five numbered actions sat orphaned inside Part 3 under a second `#### What you would do` — so RAM's remedy advice appeared under Storage.
  - **Part 4's `#### What you would do` sat after Check 4** in the walkthrough rather than inside Part 4.
  - **`## Common Pitfalls` and `## Tools for This Phase` bisected the lesson**, so Parts 7–9 rendered *after* the pitfalls list and the tools heading, with the tools table landing far below its own heading.

  Repaired by moving each block to its correct home. **Verified output-preserving:** `it.json` is byte-identical to a pre-edit baseline apart from `generatedAt`, and phase 1's counts are unchanged at 5 tasks / 7 checklists. Also folded the legacy `### Hands-On Tasks` block (the other open item) into `## Hands-on practice tasks` as check-referenced steps, preserving its detail rather than deleting it.

  Depth added in the same pass, taking the lesson from ~4,400 to ~23,400 words across 13 parts:
  - **Part 6 — reading the evidence hardware leaves behind**: S.M.A.R.T. attribute table with the "direction, not absolute numbers" rule, why memory faults are intermittent and a passing test proves nothing, a BSOD stop-code reference, where bug check and power-loss history is stored, and driver signing.
  - **Part 10 — getting in locally, then going remote**: the two stages framed as sequential rather than competing. Local employer types (BPO, shared-services/capability centres, MSPs, in-house) with the point that night-shift experience is an *asset* for later remote work; what local employers screen for; then the overseas market — role types open to PH-based applicants, EOR versus independent contractor, payment mechanics, timezone conversion tables treating UTC+8 as an advantage, and the remote reliability bar.
  - **Part 11 — working with real users**: the eight-stage ticket lifecycle and why skipping verification causes reopens, priority versus severity as independent axes, escalation and what a handoff note must contain, SLAs from zero, handling frustrated users with a weak-versus-better phrasing table, remote support consent and credential hygiene, and a weak-versus-strong ticket-note comparison.
  - **Part 12 — a worked ticket set**: five complete tickets (slow PC, dead second monitor, "won't turn on", full disk blocking updates, dropping Wi-Fi), each with the questions asked, real command output, one-change-at-a-time actions, verification, a final note, and the reasoning to take away — with later-phase boundaries stated explicitly rather than faked.

  Also: self-check extended from 8 to 21 questions, skills 6 → 11, tools 6 → 10, resources 4 → 11, and goal/estimated-time updated to state that the phase's exit criteria remain hardware-only so a beginner cannot be gated by the career material.

- [x] **IT Phases 2–4 depth pass — the standard extended beyond Phase 1.** Phase 1's treatment (evidence reading, diagnostic ladders, worked tickets, a prevention habit) was unique to it; it now covers the four foundational IT phases. All Markdown-only and **verified pipeline-neutral** — `it.json` phase counts unchanged from a pre-edit baseline, task IDs still 129, lint 81 files / 0 issues, smoke 31 renders, production build clean.
  - **Phase 2 — Operating Systems, 4,337 → 9,709 words.** Reading Windows like a technician (process trends and leak detection, the Status-versus-StartType gap, grouping events by provider and ID); reading Linux the same way (`ps aux` including the `D` storage clue, `systemctl status` as verdict-plus-reason, journal filtering, the permission triad); five interpretation exercises; two worked tickets (a memory leak misdiagnosed as needing RAM; "all my files are gone" as a stale token after a password change); and a prevention habit. **Phase 2 was also the only phase file in the repository missing both required closing sections** — it now has Key takeaways and Practice this next, matching the other 16.
  - **Phase 3 — Networking Basics, 3,370 → 16,680 words.** Subnetting taught without fear, with two fully worked examples and the one-`/24`-into-four-`/26`s table; the six-rung diagnostic ladder with one command per rung and an explicit "healthy looks like"; latency and packet-loss interpretation; the Wi-Fi "full bars but no traffic" case; a symptom-to-rung decision table; an explicit out-of-scope boundary with a model escalation note; seven walkthrough checks; and two worked tickets (a DNS failure hiding as "the internet is down"; a printer ticket that is really three `/26` subnets).
  - **Phase 4 — Helpdesk Skills, 6,137 → 9,482 words.** Identity verification treated as the security control it is (why the service desk is the attacker's front door, a verification-strength table, and the rule that *you* initiate the callback); and three worked tickets with real evidence — including a phishing ticket taught as **contain and escalate rather than investigate**, with the phase boundary stated explicitly.

  The remaining gap: Phases 5–9 have not had this treatment, and the eight cyber phases have not been assessed against it.
- [x] **IT Phases 5–9 depth pass — the standard now covers the whole IT track.** Phases 1–4 were deepened first; 5–9 now match, taking the IT lesson prose from roughly 32,800 words to roughly 97,300.
  - **Phase 5 — Sysadmin Basics, 3,039 → 13,642 words.** Reading access evidence (`whoami /groups` versus the directory as the stale-token diagnosis, `icacls` scope flags including the `(OI)(CI)(F)` and `(IO)(F)` trap, the four rules of effective access); backup truth (why a sync is not a backup, why an untested backup is not a backup, restore as the only proof); six guided checks including deliberately creating a Deny and proving it wins; two worked tickets in which the permissions were already correct.
  - **Phase 6 — Tools and Ticketing, 3,844 → 5,739 words.** A monitoring alert worked end to end with a runbook, its known false positive checked rather than assumed, and the judgement calls about what **not** to delete; plus the eleven-ticket pattern where every ticket looked like excellent MTTR and none found the cause.
  - **Phase 7 — Soft Skills, 3,343 → 5,759 words.** De-escalation as a sequence rather than a talent, a "do not say" table with reasons, owning your own mistake in active voice, and two interview answers taken apart in full. Also fixed content that sat after `### Practice this next`.
  - **Phase 8 — Portfolio and Resume, 3,875 → 6,255 words.** A complete annotated resume with the reasoning for each line, the accurate ATS story, LinkedIn as close to mandatory for remote work, and the five-question drill for defending every bullet.
  - **Phase 9 — Job Application Plan, 3,757 → 6,224 words.** The application materials written out, remote interview mechanics, and rejection sorted by stage into a specific fix.

  The remaining gap: the eight cyber phases have lessons (4,510–8,929 words) but have never been assessed against this standard.
- [x] **Published the site to GitHub Pages** (D-009). The repository went public — a full-history audit found no secrets in any revision — so Pages is free and needs no third party. A `deploy-pages.yml` workflow builds with `VITE_BASE=/cs-roadmap/`, because a project site is served from a subdirectory and Vite's absolute `/assets/...` URLs would otherwise 404 and render a blank page. The workflow verifies the emitted HTML and fails if the prefix is missing, and that guard was tested against both a prefixed build (passes) and an unprefixed one (fails). Netlify remains configured and working as a fallback host.
- [x] **Seven glued headings fixed, and the defect class automated away.** A fence-aware audit of all 17 phase files found seven headings with no blank line before them, so Markdown folded them into the preceding paragraph and they never rendered as headings — Phase 1's defects were not unique. Fixed in `02-phase-operating-systems.md` (Parts 2, 3, 4), `03-phase-networking-basics.md` (Part 2, Part 5, and the mandatory `## Tools for This Phase`), and `04-phase-helpdesk-skills.md` (Part 5). One blank line inserted per site, zero prose changed. **Verified JSON-neutral:** phases 2, 3 and 4 are byte-identical to the pre-fix baseline. The class cannot recur — `scripts/lint-content.mjs` now checks heading spacing, fence-aware, and was verified by negative test (reintroducing the defect fails the lint, naming the file, line, and heading). The same audit found no empty sections, no duplicate headings, and no bisected lessons in either track; the cyber track is clean on all three.
- [x] **Build-parser hardening — fences are now respected.** `scripts/build-content.mjs` matched `^## ` and `^### ` without tracking fenced code blocks, so a report template inside a ```text fence was read as real phase structure. Phase 6's lesson exposed this: an *unindented* template containing `## Summary`, `## Findings`, and `## Checklist` produced eleven phantom sections and a "checklist line without an id comment" failure. The `sections()` and `subsections()` scanners now track fence state, a heading inside a fence is never a section, and `docs/CONTENT-GUIDE.md` documents the rule plus the unterminated-fence failure mode it introduces. **Verified output-preserving:** both `it.json` and `cyber.json` remain byte-identical to a `HEAD` baseline worktree. **Verified by negative test:** with a fenced `## Checklist` injection, the old parser failed the build (exit 1, no output) while the new parser built cleanly and ignored it. The Phase 6 template was then restored to unindented form, since the workaround is no longer needed and the template now copies cleanly.

- [x] **Cyber depth pass — the thin phases brought up to the standard.** Phases 4, 5 and 6 were the shortest lessons in the track (4,788 / 5,200 / 5,828 words) against 8,467–9,595 in phases 2, 3 and 7. All three now sit in the 9,000–9,500 band, and every phase in the track is in that range.
  - **Phase 4 — Hands-on Labs, 4,788 → 9,045 lesson words.** The phase that teaches *doing the work* was the thinnest. Added an end-to-end lab build (hypervisor comparison, host preparation, resource allocation on a modest laptop, VM networking modes and when each is right, snapshots as deltas, safe teardown); a twelve-row troubleshooting table for the failures beginners actually hit (no internet in the VM, NAT-versus-bridged confusion, disk exhaustion, guest additions, nested virtualisation, an agent not checking in, clock skew, snapshot restore failure); evidence capture and file-naming conventions with a worked example of one alert documented properly; isolated malware handling, including why a bridged adapter is never acceptable and the legal boundary; and a lab notebook template.
  - **Phase 5 — Specialization Choice, 5,200 → 9,329 lesson words.** Added a weighted decision matrix and a worked example choosing between SOC and GRC for a concrete low-spec profile; "what the first 90 days actually look like" for all four paths, described as daily work rather than job title; a skills-transfer matrix with the 70/30 pivot rule and pivot cost; and honest Philippine market observations. Also fixed a pre-existing duplicate `### Part 3` heading.
  - **Phase 6 — Portfolio Projects, 5,828 → 9,311 lesson words.** Added a fourth project (threat-intelligence briefing) with full specification; a worked weak-versus-strong report excerpt with annotated reasoning; repository presentation as a 60-second scan with a README template; per-project interview follow-up questions with model answers; and how to write about employer-owned or NDA-bound work honestly.
- [x] **Six new cyber modules — the gaps the original eight phases had no home for.** Numbered 09–14 so the existing ordering is untouched. Each follows the full section contract, runs 8,000–10,000 words, and stays $0: **09 Cloud and Identity** (shared responsibility, IAM and least privilege, Entra ID, conditional access, metadata-service SSRF, cloud logging); **10 Detection Engineering** (log sources, Windows event IDs, Sysmon, auditd, writing and tuning rules, false positives, Sigma, ATT&CK mapping, alert fatigue); **11 Incident Response and DFIR** (the NIST 800-61 lifecycle, triage and severity, order of volatility, evidence handling and chain of custody, imaging, timeline analysis, containment, report writing, tabletops); **12 Scripting and Automation** (Python and PowerShell applied to triage, parsing logs and JSON, reading APIs, handling untrusted input, when *not* to automate, secret handling); **13 GRC, Compliance and Risk** (risk registers and scoring, ISO 27001 / NIST CSF / SOC 2 / PCI DSS, control mapping, evidence and audits, policy writing, third-party risk, the Philippine Data Privacy Act); **14 Web Application Security** (the OWASP Top 10 with real examples, HTTP and session flaws, injection and access-control bugs, Burp Community and ZAP, and a written finding with remediation).
- [x] **Surfaced the lessons in the site UI** (D-011, D-012). The old note here said this "needs a deliberate decision about rendering and payload size, not just a data change" — both turned out to be true, and both are now decided and implemented. `scripts/lesson-ast.mjs` parses the lesson region into a block AST at build time, `Lesson.jsx` renders it behind a table of contents with scroll-spy, and lessons ship as per-phase files loaded on demand. The payload concern was real: inlining every lesson pushed the JS bundle from 265 KB to 1.96 MB, so splitting it back out to `generated/lessons/` (D-012) returned the initial bundle to ~355 KB with each lesson a 30–170 KB chunk fetched only when opened. Two guards protect the path: `audit-lesson-ast.mjs` proves no lesson loses content in parsing, and `smoke-render.mjs` renders every lesson and asserts tables, code, and headings all appear.
- [x] **Fixed the sidebar scrolling away, and made it a drawer on small screens** (D-010). The sidebar had no height or position constraint, so as a flex item it stretched to the full document height and scrolled out of view — on a long lesson the reader had to scroll back to the top to navigate. It is now viewport-pinned with its own scrollbar. Below 860px it becomes an off-canvas drawer behind a hamburger button instead of stacking above the content, with Escape, backdrop-click, and scroll locking. View changes also reset scroll to the top.

## Next

- [ ] **Push `main`.** 8 commits — the lesson renderer, the six new modules, and the sidebar fix — are committed locally but not yet on `origin`. CI has therefore not run against them.
- [ ] **Split the dense paragraphs.** Every phase passes the readability audit, because it gates on a per-phase average. Measured per paragraph, **33 IT paragraphs exceed 90 words** (9 over 110, worst 193 in Phase 1) and the six new cyber modules add 12 more (worst 162). Worth doing alongside a change to the audit so it cannot regress.
- [ ] **Audit the six new modules against the depth standard.** Modules 09–14 meet the section contract and every readability target, but have not had the evidence-reading and worked-ticket review that IT Phases 1–9 and cyber Phases 1–8 received. An audit first, a writing pass second.
- [ ] **Assess the IT track's thin phases.** IT word counts range from 6,219 (Phase 7) to 24,723 (Phase 1). Phase 1 is deep because it was the pilot; whether the later phases deserve the same treatment, or the range is appropriate, needs a judgement rather than an assumption.

## Later / optional

- [ ] Full-text search across lessons, if the curriculum outgrows the sidebar and per-phase navigation.
- [ ] Print stylesheet for a lesson, so a phase can be taken offline on paper.
- [ ] Gate the readability audit on per-paragraph density, not only the average — `longest para` is already computed and simply not enforced.

## Explicitly out of scope

- Paid tools or resources that require a budget.
- Anything that adds a runtime dependency to read the content.
- Scope creep that makes the curriculum feel unachievable for a beginner.
- **Word-count ceilings on lessons.** 3,000 words is a floor. Longer, more detailed lessons are better; never trim a lesson to hit a target.