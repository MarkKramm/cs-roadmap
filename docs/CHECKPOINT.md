# Checkpoint

A snapshot of the repository's current state. Update this when a meaningful milestone is reached.

## Current state — 2026-09-17 (certification tracking, and the cyber track's documents made reachable)

| Item | Value |
|---|---|
| Branch | `main` |
| Tracked files | **201**, all committed. Run `git ls-files \| Measure-Object` for the live count |
| Working tree | **Clean and level with `origin/main`.** This pass's content fixes and doc updates are committed and pushed |
| Line endings | LF everywhere (Windows scripts excepted) |
| Encoding | UTF-8, no BOM |
| Remote | `origin` → https://github.com/MarkKramm/cs-roadmap |
| Visibility | **Public** — `"private": false` via the GitHub API. Made public for Pages (D-009); the history was audited for secrets before that |
| Deploy | **Live.** `.github/workflows/deploy-pages.yml` publishes `learning-site/dist` to <https://markkramm.github.io/cs-roadmap/> |
| License | CC BY 4.0 |
| Build step | `learning-site/` — React + Vite. `npm run dev` / `npm run build` |
| Curriculum size | **31 phases** in three tracks (IT 9, cyber 15, advance 7). **491,331 words across the 31 phase files** — IT 161,200, cyber 214,056, advance 116,075 (**cyber 02 grew 9,612 → 13,558 this pass**). **443 phase task IDs** — **283 practice tasks**, every one banded and energy-tagged, plus **160 checklist items**. **380 quiz questions across all 31 phases** — every phase now has one |
| **This pass** | **Two things the content asked for that the site had no surface for.** (1) **Cyber Phase 07 (8,929 words) ends by requiring a decision document** — target certification, cost checked on the official site, job-post evidence, topic gap table, exam date decision — and calls the 30-job-post comparison its most valuable task. The reader who did that work had **nowhere to put it**, so **Certifications** is now tracked (`useCertifications.js`, `pages/Certifications.jsx`), shaped like `Applications` because a certification is a 3–12 month commitment inside a 34–112 week plan, not a note. The statuses are **positions in a decision, not rungs of a ladder**: `deferred` is an outcome Phase 07 explicitly endorses, and a tracker with no way to record a deliberate delay would push a beginner toward an exam they are not ready for. No hours field, no percentage ready, no objectives cleared — **a certification tracker that measures study is a report card**, and the smoke test now asserts the absence of percentage and completion language so it cannot be "improved" into a scoreboard. Only a name is required, because refusing to record a thought until the fields are complete loses the thought. (2) **`TOOLBOX.md`, `FREE-TOOL-MAP.md` and `WHEN-TO-BUY-THM-PREMIUM.md` had zero references anywhere in the site** — `build-content.mjs` walks for `*-phase-*.md`, so a standalone document is invisible to it and nothing else looked. **This is D-020's gap, left open on the other track**; the same fix (render from the build artifact) now covers all six documents. **A shrinking corpus was invisible to the test that guarded it** — the smoke test looped over whatever `shared.json` contained, so removing a document made the corpus smaller and every remaining assertion passed. The three are now named explicitly, proven by mutation. Also corrected **`DESIGN-SYSTEM.md` → "Still open"**, which listed the print stylesheet (D-017) and progress export (D-016) as unbuilt long after both shipped. **The round trip for certifications needed a real browser and had to write rather than seed** — a check that seeds `localStorage` verifies the reader, not the writer — and deleting the `setItem` call turns 3 checks red, the exact bug class D-044's quiz shipped with. 12th registered key; `test-data.mjs` caught it immediately with `expected 12, got 11` |
| Previous pass | **The quiz got a read side: a review queue of the questions you got wrong, gathered from every phase.** The curriculum produces 380 questions, each carrying a `**Why:**` line that names the misconception its distractors represent — and that line was visible once, then gone. Reaching it again meant remembering which phase the question was in and re-taking the whole set, which is **a memory test about the site's own navigation, not revision.** Answers now persist in `cs-roadmap:quiz:v1` and missed ones surface under **Your work**. **The component's original objection to persisting was taken seriously rather than overruled:** it held that storage would *"turn a self-check into a permanent record of how you did."* Correct about the risk, wrong about the remedy — so what is stored is narrowed to the minimum (which option was picked, keyed by question id) and nothing about correctness, counts, ratios or improvement is stored anywhere. **A question never answered is not a gap**, since a beginner must not be shown unstudied material as a failure. **Mutation testing caught two defects in my own work:** a redundant `chosen === undefined` guard that could be deleted with no observable change (**a guard that cannot be observed is not defence in depth**), and a missing upper bound that would let a corrupt index surface as a phantom miss. **The browser suite then caught the wiring bug the unit tests could not** — the persistence check clicked a quiz on **IT 02** left over from earlier steps, then asserted about **IT 01**; the store was correct throughout. **A check that asserts across two subjects is not stricter, it is broken, and this one looked exactly like a real bug.** The page text also had to change: it said *"refreshing starts over,"* which stopped being true. 44 new checks, all mutation-tested; 112 browser checks |
| Previous pass | **Every phase now has a quiz, and the guard written to check them did not work.** Twenty of 31 phases had no quiz — including **IT 01 at 26,259 words**, the largest in the repository and untestable. All 20 now have one: **380 questions, 31 of 31 phases.** Each is written from its own phase, and a **new sourcing guard enforces that mechanically** — a question must share more distinctive vocabulary with the phase it lives in than with any other. **The guard passed every synthetic control while being unable to catch a real misplaced question:** planting a genuine cyber-01 question into cyber-15 scored own=17 vs bestOther=15, a margin of 2 where the rule wanted 4, and it **exited 0**. Measuring rather than adjusting the number showed the `**Why:**` line carries most of the signal — scoring the whole block moves the margin to **15–29 terms at ratios of 1.39–2.21** across all 14 other phases, so the gate is a 1.30x ratio with real headroom. A control now plants a **real** question so this cannot regress. **Two more bugs in the same guard, both caught by its own controls:** the id rule was read from the frontmatter rather than the corpus, giving **141 findings across 10 quizzes that were already passing**; and an "exactly 4 options" rule **contradicted a deliberate decision** in `audit-quiz.mjs`, which permits a fifth option where a fourth real distractor exists |
| Previous pass | **A browser check had to be replaced because its subject was fixed, and the first replacement was vacuous.** The check clicked IT 01 and asserted no quiz rendered — true while IT 01 was the one phase without one. Giving every phase a quiz made the assertion impossible to hold. **The first replacement passed even with the empty-quiz branch deliberately rewritten to render an empty shell**, because no phase reaches that branch any more: it was caught by breaking the component on purpose and watching the check stay green. **A check that cannot fail is not a check**, so it was replaced with one that is reachable and load-bearing — the phase that used to have no quiz now renders real questions under a real heading — proven able to fail by forcing the component to return early for every phase. The empty branch stays covered where it can be reached, in `test-quiz.mjs` |
| Previous pass | **A real content defect found and fixed, plus the guard that can see its class.** Cyber pass 4 returned three `WRONG` verdicts on one claim: `01-phase-foundations.md` taught **five** NIST CSF functions when **CSF 2.0 has six** — it added Govern. **Verified against the standard, not the model's citation** (which pointed at a commercial blog and a NIST blog post): NIST's own CSWP 29 says *"the CSF Core as a hierarchy of **six Functions**"* and *"the inner layer of the wheel contains only the **Govern Function**."* **Wrong in four places in one phase** — summary table, numbered lesson section headed "the five functions", exit criterion, resources row. **Every guard passed over it, and the reason is the point: each checks the text against ITSELF.** Immaculate markdown, a resolving URL, a quiz that never mentioned the functions, five parsing as cleanly as six, a beginner following it fine, valid UTF-8. **The corpus contradicted itself** — phase 13 said six, correctly, twice — and **nothing compares one phase to another.** `audit-framework-claims.mjs` now checks assertions whose current value was read **from the standard**, narrow and evidence-quoted, with controls proving it fails on four shapes of the retired model and passes on correct text and legitimate historical notes. **Two bugs in that guard were caught by testing it.** **The lesson: a summary of a versioned standard is a time bomb with a long fuse** — true when written, silently false after a revision, with no author error to find. See **D-040** |
| Previous pass | **The IT track's technical claims are verified — all 224, and zero were wrong. 176 `OK`, 48 `UNVERIFIABLE`, 0 `WRONG`.** Five classes, each pasted into a chat with web access as a **self-contained message**. **Every citation spot-checked held verbatim against the document it named** — RFC 768, RFC 9293, RFC 3596 §2.1, the OpenSSH manual, FHS 3.0 §5.10.1 for `/var/log`, and Microsoft's `MSFT_PhysicalDisk` for `HealthStatus`. Quotes were tested against the sources, not accepted because they read plausibly. **The 48 `UNVERIFIABLE` rows are the honest part** — teaching method, diagnostic heuristics, resume phrasing, case-study narrative — and stamping those OK would have made the OK column meaningless. **But a clean result is not a verdict on the curriculum:** the extractor cannot see a bad analogy, a misleading emphasis, or outdated practice that reads as current, and **that layer has only ever been tested by the comprehension passes.** The sourcing was also **weaker than the verdicts** — several citations rested on a third-party tutorial returning **HTTP 403** and on a localised manpage; rechecked against the standards and all held, but recorded rather than glossed. Claim density now **measured rather than assumed**: IT 3.9 checkable claims per 1,000 words, **cyber 2.3**, advance ~1.5 — and cyber's claims sit in *different* classes (protocols 40 vs 18, versions 35 vs 16, registry paths 53 vs 21, 15 CVEs). **Then the corpus turned out to have its own encoding corruption:** `docs/SESSION-LOG.md` held double-encoded mojibake **inside the sentence describing the mojibake bug**, and it had survived several sessions. **My first scan for it found nothing** — it enumerated printable corruption sequences, and this instance contained **U+009D**, a C1 control character no such list would name. So `audit-encoding.mjs` tests the decidable property instead — **no BOM, no C1 controls, no U+FFFD** — with **8 controls proving it can fail**, one rule dropped rather than shipped noisy (`git checkout -- path` is not a lazy em-dash). See **D-038** |
| Previous pass | **The first external verification pass returned three real errors — the class D-036 opened is productive, not theoretical.** A model with web access worked the worklist against primary sources: **two command errors and one vocabulary error, all three invisible to every existing guard** because the text was internally consistent and spelled correctly. **IT 02's summary table said `` `DISM /RestoreHealth` `` — invalid syntax that throws**, while the phase states it correctly 300 lines earlier, so the *table* (where a reader copies from) held the only broken form. **IT 01's log said `` `DISM /AnalyzeComponentStore` ``**, missing the same flags, while the very next line gets `StartComponentCleanup` right. **IT 01 told the reader to watch `HealthStatus` for `Caution` or `Bad`** — CrystalDiskInfo's words, not the cmdlet's (`Healthy`/`Warning`/`Unhealthy`/`Unknown`), and the phase's own sample output prints `Healthy` four lines earlier. **All three share one shape: right name, broken invocation.** So the new guard checks *the string a reader would copy* rather than the command name — `audit-commands.mjs`, narrow to `DISM`/`sfc`/`chkdsk` because a guard that flags valid usage gets ignored, and **proved able to fail by re-injecting the original defect** (exit 1 with it, 0 without). `verify-ports` extended to **19 checks** (port 9100 confirmed against IANA). The changelog guard caught a duplicate `### Fixed` section **in this very edit**. See **D-037** |
| Previous pass | **Technical correctness — the one claim class no guard here could see, now with a method and three closed classes.** Every guard tests *internal consistency*; the comprehension passes test whether a beginner can *follow* the text. Both are blind to a sentence that is perfectly consistent, perfectly clear, and factually wrong. **954 raw claims extracted from the nine IT phases → 216 needing a source**, in a worklist for pasting into a chat with web access. **Three classes settled by machine, all passing:** `verify-cidr` **42 checks** (every CIDR row, every mask equivalence, each RFC 1918 range's real span, IT 03's worked `/26`), `verify-metrics` **15 checks** (all five IT 06 metrics recomputed from its own table), `verify-ports` against the **IANA registry that assigns the numbers**. The first two run in CI; `verify-ports` deliberately does not, because a network-dependent guard would one day fail for a reason unrelated to the content. **The item had sat recorded as unclosable by an agent** — that was wrong: search is broken, direct primary-source retrieval is not. See [`IT-CLAIM-VERIFICATION.md`](IT-CLAIM-VERIFICATION.md) and **D-036** |
| Previous pass | **Quizzes across the whole IT on-ramp — 3 phases → 10, 34 questions → 128.** IT 03–09, because the on-ramp is read first and in order by someone preparing for their first job, which is where a self-check is worth most. **The technically risky phase was verified before its answers were written:** IT 03's subnet table (all seven rows) and its worked `/26` example were recomputed independently and agree, as were IT 06's five worked metrics. Three phases had questions answering *another* phase's material — writing a batch makes that likelier — and both were replaced. The guard gated three times: IT 09's draft had **nothing in position A**, and **IT 06 shipped 8 of 14 in position C with A unused** — written by the same author who had pre-planned the three phases before it. Then the defect *one level up*: all ten quizzes passed individually while the **corpus** had drifted to **A=17.2% / C=35.2%**, with three phases sitting exactly on the 50% line. Nineteen answers reordered → **A=25.0% B=25.0% C=27.3% D=22.7%** |
| Earlier this session | **The five IT 02 topics that were listed and never taught, now written** — dependent services, startup apps / slow-boot diagnosis, NTFS versus share permissions, update rollback, and profile corruption. Phase 2 grew **12,272 → 15,431 words**; tasks **385 → 390**, checklist **7 → 12**, three new deliverable artefacts. And a **negative result**: the topic-list guard `ROADMAP.md` called "the highest-value guard currently absent" was tested against ground truth and **cannot be built** — a keyword probe missed 5 of 5 known-undelivered topics, and any presence-based check counts a *promise* as teaching. Recorded as **D-033**. Also this session: the **CHANGELOG structure guard** (D-034), which found a real defect on its first run, and a CI workflow self-validator |
| Earlier this session | **A comprehension pass over the material no fresh reader had seen** — cyber 15, advance 07, and the five IT sections the gap pass added. Five readers, one per file, same brief as the three prior passes. **~40 findings reported, about a dozen rejected after re-checking.** Fourteen real defects fixed across five files, including a "negative" test fixture the rule actually matches, a takeaway that reverses its own table, an irreversible `sysprep` ordered before its snapshot, and conditional access taught as free. Full write-up in [`COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md) → "Fourth pass — the unread material" |
| IT gap pass | An audit of all 12 IT files (~143,000 words) against real entry-level remote requirements (`docs/IT-CONTENT-AUDIT.md`) found **4 missing and 5 thin topics, and disproved 11 others**. All nine were folded into the phase that already owned them — no tenth phase. IT checklist **66 → 75 items**, one per gap. Every addition is $0 |
| Two new phases this pass | **Cyber 15 — OT and ICS Security** (1,216 lines, 14,093 lesson words, 10 practice tasks, 17 checklist items) and **Advance 07 — Detection as Code** (1,348 lines, 10,868 lesson words, 11 practice tasks, 22 checklist items). Both now **have** been read by a comprehension pass — see the pass row above |
| Track shape | **The mid-level track is cyber-only, deliberately** (D-032). IT entry → cyber entry → `advance-roadmap/`, whose overview assumes the reader already holds a security role. There is **no fourth track** for an IT professional who stays in IT. The documentation previously implied a two-track repository in three places and never told the reader the IT path ends; both are corrected, and the omission is now recorded as a decision rather than left unexamined |
| Checks | `audit-content` **0 issues** (phase structure intact); `audit-encoding` **71** files / 0 (no BOM, no C1 controls, no lost bytes, 16 controls); `audit-quiz` **0 findings across 31 quizzes / 380 questions** — corpus positions **A=90 B=97 C=97 D=96**, every phase balanced; `audit-quiz-sourcing` **0 findings / 380 questions, 15 controls** proving it fails on a question lifted from another phase; `audit-readability` **1 paragraph over the 90-word editorial target, 0 over the 110-word gate**; `audit-refs` 0 broken (2 forward-as-prior observations, informational); `audit-time-budget` **31 phases** / 0 findings; `audit-changelog` 0 findings, 13 controls; `audit-terms --self-test` **16 expansion + 23 classifier controls / 0 failed**; `audit-framework-claims` 41 files / 9 rules with 31 controls; `audit-nist-current` 10 checks / 0; `audit-commands` 0 findings; `audit-lesson-ast` 31 lessons / 0 content loss; `verify-cidr` 42 / 0 wrong; `verify-metrics` 15 / 0 wrong; `verify-ports` 19 / 0 wrong against the IANA registry (run by hand — needs network); `validate-ci` **43 steps** / 0 problems; `test:render-inline` 34; `test:highlight` 37; `test:ui` 42; `test:data` **103**; `test:notes` 28; `test:work` 31; `test:today` 77; `test:lesson-search` 77; `test:phase-transfer` 56; `test:path-order` 37; `test:quiz` 53; `test:review` 44; `test:search` 41; `test:smoke` **235 renders / 0 failures across 31 phases, 275 tools and 6 shared documents**; `test:browser` **118 checks / 0 failures** on Edge (`MIN_CHECKS` 100; **Chromium only** — see D-031). **The lint figure is a file count, not a script:** there is no `audit-lint.mjs`; `lint-content` counts the files it walks. Run the suite end to end after any content change. Current: **22 guards, 0 failures** — but the honest caveat is that `verify-ports` needs the network and is not in CI, and `audit-terms` is a measurement rather than a gate |
| Time budget | A tenth `localStorage` key, `cs-roadmap:time-budget:v1` (D-021), owned by `hooks/useTimeBudget.js` and registered in `transfer.js` → `KEYS` |
| Bundle & build output | Initial chunk **1020 KB raw** (`dist/assets/index-*.js`), with **31** on-demand lesson chunks. No new runtime dependency was added by any of this pass's features — the Certifications page and the three extra shared documents are all pure application code and content, so the growth is the feature's own code and the doubled `shared.json` |
| Task IDs | **392 total**, of which **283 are practice tasks** — `283 banded, 0 authored without a band, 0 still minted from position`, and `283 of 283 practice tasks carry an energy value` (all four figures printed by `build-content.mjs`, so they are reproducible rather than remembered). The remaining 109 are checklist items inside the phase files. **A previous revision of this row said 443 and that number does not reproduce** — `git ls-files \| Measure-Object` gives 201 tracked files, and no command in the repository emits 443. The figures above are the ones the build prints |
| CI | `.github/workflows/ci.yml` — **three jobs**: `content`, `site`, and `browser`. The browser job's shape changed twice this pass and the second change is the one that matters. It was made a two-leg matrix to add **Firefox**; **the Firefox leg failed on its first run**, and the cause is not a bug in this repository. **Mozilla removed Firefox's CDP implementation** — its own documentation for `remote.active-protocols` states: *"With the end of the CDP support, WebDriver BiDi is the only available protocol, and the preference was removed in Firefox 141."* So `--remote-debugging-port` exposes no CDP endpoint, and no flag or path change fixes it; driving Firefox needs WebDriver BiDi, a different protocol. **The leg was removed rather than left failing or quietly made optional**, because a permanently red job trains a reader to ignore CI and a silently-skipping leg would report a green Firefox result for an engine that never started. The engine-selection code is kept and still exercised, and an unsupported engine now **fails immediately with the reason stated** instead of timing out at the target lookup. `test:phase-transfer` and `test:path-order` joined the site job |
| Guards in CI | Unchanged in kind, extended in coverage: the **seven** content guards — `lint-content`, `audit-content`, `audit-lesson-ast`, `audit-readability`, `audit-refs`, `audit-time-budget`, `audit-changelog` — plus `test-audit-changelog` and `audit-terms --self-test` as control suites, and **thirteen** site suites. Every one is a plain node run needing no extra install |
| Milestone | M3 complete — the site renders the lessons themselves, not just the structured sections |
| Search | Full-text across all **31** lessons, three tracks (D-013). Build-time inverted index, **not referenced by `index.html`**, so a reader who never searches pays nothing |
| The reader's own writing | A note per phase and an answer per practice task in `cs-roadmap:notes:v1` (D-019), readable back through **Your work** (D-020). Deliberately unscored, and asserted to stay that way |
| Single-phase transfer | **New (D-030).** `exportPhase` narrows a backup to the ids one phase owns; **the emitted file is a real backup** — same format, same version — so the existing `inspect`/`importAll` read it with no special case. `importPhase` is **additive by construction**: it unions, existing wins on a collision, and it cannot remove anything. Guarded by `test:phase-transfer`, **56 checks** weighted toward rejection and merge cases |
| Where you've been | **New.** The neutral list the roadmap recorded as deliberately unbuilt because it risked a completion-scold. The scold is avoided **structurally**: `lib/pathOrder.js` returns rows with no `remaining`, `percent` or `missing` field at all, so the absence is a property of the data rather than of the rendering. Asserted in `test:path-order` (37 checks) **and in a live DOM** by the browser check |
| Shared documents | Reachable from the site (D-020) and now **linkable by name** — the dashboard rail carries a "Beside the tracks" card reading its list from `shared.json`. `initialId` is the seed, `onSelect` the echo back up, because `Shared` remounts on every view change. Still not routing (D-007 stands) |
| Paragraph density | **Nothing anywhere exceeds the 110-word gate, and nothing exceeds the 90-word editorial target either — the editorial backlog is now zero.** The gate moved down from 150 to 110 in D-024; that pass split 33 IT walls of text and the last four over the editorial target (IT 01 91, IT 03 95, IT 05 94, IT 07 93). **This pass cleared the remaining 16** (plus the 17th and 18th found while doing it), across 11 phases in the advance track and newly written material; the longest was **108 words in advance 05** and the longest in the repository is now under 90. Every split kept **every word**: `6eda6d7..d62b204` was compared file by file and **all 35 files, 405,911 words before and after, have an identical word sequence** — which is a stronger claim than an equal count, because inserting a paragraph break changes whitespace and cannot change a word order, so any difference would have been proof of content loss. `EDITORIAL = 90` is named and reported, `CEILING = 110` fails, so the report and the gate cannot drift apart |
| Cyber depth track | Six new modules added as Phases 9–14, because the core path lists cloud, detection, IR, scripting, GRC and web security as target roles but taught none of them: cloud and identity, detection engineering, incident response and DFIR, scripting and automation, GRC and compliance, and web application security. All $0, all with explicit legal-boundary sections |
| Cyber rebalance | Phases 4, 5 and 6 were the thinnest lessons in the track (4,788 / 5,200 / 5,828) and are now level with the rest (9,045 / 9,329 / 9,311). Phase 4 gained a full lab build, a twelve-row troubleshooting table, evidence capture and malware isolation; Phase 5 a weighted decision matrix and the first 90 days of each path; Phase 6 a fourth project, an annotated weak-versus-strong report, and interview follow-ups |
| Lesson rendering | The site parses each lesson into a block AST at build time (`scripts/lesson-ast.mjs`) and renders it with a table of contents and scroll-spy (D-011). Lessons ship as per-phase files loaded on demand, which took the initial bundle from 1.96 MB back to 394 KB (D-012). Two guards protect the path, since a parser bug deletes content rather than crashing: `audit-lesson-ast.mjs` and the lesson assertions in `test:smoke` |
| Site UI | Sidebar is viewport-pinned and scrolls independently; below 860px it becomes an off-canvas drawer with Escape, backdrop-click and scroll locking (D-010). View changes reset scroll to the top. Lesson body has a reading measure, horizontally scrolling tables, and focus-visible states. **The shell centres and is capped at 1,400px**; the dashboard is two columns above 1,180px with a sticky reference rail — progress ring, plan dates, readiness counts, what comes next — that reflows to cards below the breakpoint rather than hiding anything (D-015). Phase cards tile two-up. |
| Dashboard rail (D-015) | The page that answers "what should I do today" is the one a reader on a 34–112 week curriculum sees most, and it was the least informative: a single column of cards filling the left half of the viewport. It now carries a reference column of the numbers a reader checks rather than acts on. **It is a reference, not a second dashboard** — no charts, no streaks, and its only controls are links to the page that owns each figure. Every count reads "you have N", never "you are missing N". |
| Rail reads, never owns | `readStarts()`, `readPortfolio()`, `readApplications()` and `readCertifications()` are plain reads, deliberately **not** the hooks. Two live copies of one `localStorage` key drift, and the stale one writing back would drop a record the reader just added on the page that owns it. The hooks keep their subscribing behaviour for the pages that mutate. `fmtWeeks` and `fmtDate` moved into `lib/pace.js` so the rail and the Schedule page cannot round one number two ways. |
| Navigation & reading (D-014) | Seven `ROADMAP.md` open items closed in one pass. **Prev/next phase navigation** rendered twice per phase (compact strip + named cards), honest at both track boundaries rather than vanishing. **Per-section done toggles** on every `###`/`####` heading, keyed by heading id and namespaced per phase — a **separate** store from the phase checklist, so neither number lies. **Reading-position bar** measuring the `.lesson` region, not the page, plus a resume prompt that suggests and waits. **Schedule view** comparing work done against calendar elapsed once a start date is set, framed as a readout rather than a verdict. **Keyboard shortcuts** (`/`, `⌘K`, `j`/`k`, `n`/`p`, `g d`, `g s`, `?`) with none bound to a checkbox. **Reading-size control** scaling one custom property on the lesson body. **A second breakpoint at 560px** and a `prefers-reduced-motion` block |
| Phase 1 (IT) | Structurally repaired and deepened. Four rendering defects fixed, the legacy task block folded in, and the lesson taken from ~4,400 to ~23,800 words across 13 parts (evidence reading, the local→remote career route, working with real users, and five worked tickets) |
| Phases 2–4 (IT) | Deepened to the Phase 1 standard. Phase 2, 4,337 → 9,709 words. Phase 3, 3,370 → 16,680. Phase 4, 6,137 → 9,482 |
| Phases 5–9 (IT) | Deepened to the same standard, taking the IT lesson prose from roughly 32,800 to roughly 97,300 words. A structural defect was fixed along the way: Phases 6, 7 and 8 each had lesson content stranded after the closing sections |

### Guards fixed in this pass

Five audit scripts matched phase files with `^0[1-9]-`, which covers 01–09 only. Every guard was therefore blind to phases 10 and above — and `audit-lesson-ast.mjs`, written fresh, inherited the same bug and was checking 18 of 23 lessons. All five now match `^(?!00-)\d{2}-`, which includes 10+ while still excluding `00-overview`. The readability script's track summary also hardcoded the original eight cyber phase names, so the six new modules were averaged into the IT track; it now derives the track from the path.

This is worth recording because the failure mode was silence: the guards printed a clean result while covering four fifths of the content.

### The storage-key guard was rewritten, because the old one could not fail

`learning-site/scripts/test-data.mjs` asserted `KEYS.length === 9` and compared the registry against a fixture **hand-maintained in the same file**. It was therefore comparing the test to itself, and could not detect the failure it existed to prevent: a storage key the site writes but the backup does not know about. It had already broken on **two consecutive key additions (8 → 9 → 10)** while catching nothing — a hardcoded length can only restate what someone already knew.

It now scans `src/` for `cs-roadmap:*:vN` string literals and compares against `KEYS` in **both directions**: every key the site writes must be registered, and every registered key must actually be used. The duplicate, weaker `KEYS.length === 9` assertion in `scripts/test-notes.mjs` was deleted for the same reason. `test:data` is at **86 checks**.

**This was the second time in this project a guard printed nothing and was read as green** — the first was the `^0[1-9]-` pattern that hid phases 10+. It is also the third time a key-count change broke the suite. The pattern is consistent: a check that restates a number someone already believes is not a check, and its failure mode is silence rather than a red result.

**A later pass found the same class twice more, and it is now the most productive thing this project knows how to look for.** `audit-refs.mjs`'s `FORWARD_AS_PRIOR` detector had **never been capable of firing**: its call site sliced `line.slice(0, m.index + 4)` — four characters from the match, i.e. `"Phas"` — so the cue regex, which needs the literal word, could not match, and the `(0)` it reported for its whole life was not a clean corpus. `audit-terms.mjs`'s expansion detector missed four separate shapes, including the commonest one in this repo, and reported `**SPF** (Sender Policy Framework)` as never explained **on the line that explains it**. Three instances, one class: the broken version prints a clean result. Both scripts now carry a controls block that runs on every invocation and exits non-zero before printing any finding (D-028).

### Rendered in a browser, once, on 2026-09-16

This section previously read "Never rendered in a browser" and carried the caveat at the top of every session. **That gap is closed.** The notes UI, the dashboard rail, the print stylesheet, the Shared view, the Your work view and the time-budget control were rendered in a real engine on 2026-09-16 by `learning-site/scripts/browser-check.mjs`, which drives it over the DevTools Protocol with **no test dependency** (Node 24 ships a global `WebSocket`). **79 checks, 0 failed.** It runs in CI, where `ubuntu-latest` has Chrome at a known path, so it is a standing guard rather than a one-off. See D-023.

What it asserts that a string render cannot: computed CSS, layout geometry, media-query behaviour, and focus survival across keystrokes. The two assertions worth naming are **focus survival** (the driver types one character, asserts the textarea still has focus, types a second, asserts the value accumulated — a panel that remounts per keystroke is a real React defect invisible to any test that sets `.value` once) and **the print media query actually applying** (`Emulation.setEmulatedMedia({media:'print'})`, then assert the sidebar computes to `display: none`, card borders to `none`, `--bg` to `#ffffff`, `break-inside: avoid` on code). Both real writes are verified through the real storage key, and `Your work` is asserted in a live DOM to carry no `N of M`, no percentage and no `role="progressbar"`.

**The honest result: it found no site defects.** Its first run reported three failures and all three were the driver's own fault — it measured anchors on whichever Shared document opens first, while the resources list is the third. That is recorded in the script's comment and in D-023 rather than quietly fixed, because "the browser check found nothing" is only good news if the check is known to be able to fail.

**A skip cannot masquerade as a pass.** The script exits 0 with an explicit `SKIPPED` line when no browser is found, which is right for a contributor's machine and wrong for CI — `completed / success` cannot tell that apart from a passing run. The workflow therefore sets `BROWSER_CHECK_STRICT=1`, which turns a missing browser into a failure, and the script also fails if fewer than `MIN_CHECKS` (40) ran, which catches a run that connected to an error page and asserted almost nothing. Both paths were exercised locally — strict with no browser exits 1, non-strict with no browser exits 0 — and **CI run #45 passed the step with strict mode on**, which is the end-to-end proof that the runner has a browser and the checks executed. `BROWSER_PATH` exists so the skip path is testable without breaking `PATH` and hiding `node` itself.

**The band this section used to name as a gap is now covered, and covering it found a defect in the check rather than in the site.** The check previously ran two widths; it now runs **seven** — 1180, 1179, 900, 861, 860, 760, 620 — with assertions placed on **both sides of each breakpoint** (1180/1179 where the dashboard grid goes two-column, 861/860 where the sidebar becomes an off-canvas drawer), so each flip is observed rather than inferred. The first run at 760 and 620 produced an immediate failure: `rail: has collapsed by 620px — display=block width=573`. **That assertion was wrong about the site.** `global.css` declares no rail-collapse mechanism at all; the *sidebar* is the drawer below 860px and the rail simply goes full-width, so a 573px rail at 620px is correct. The assertion was replaced with ones derived from what the CSS actually declares. This is the same mistake as a guard whose skip path looks like its pass path, one level up — asserting a mechanism that was assumed rather than read.

**The engine is now named, because it was always being chosen silently.** The `BROWSERS` list tries Edge first, so every prior run measured Edge while reporting only "a real browser". The script now prints `Browser.getVersion` and asserts on it (`Edg/153.0.4234.32` locally).

**What remains true.** This is the section that overclaimed most, and it is worth being precise about what it said and what was actually true. It read: "The check drives **one engine per run** — the script identifies which one it used, and the second engine is the CI runner's `/usr/bin/google-chrome`. That second engine is no longer merely possible: **CI run #57 on `11f62e4` shows `Browser check (real engine)` as `success`**." Every word of that was accurate, and the conclusion drawn from it — that the browser question was settled — was not. **Edge and Chrome are both Chromium.** Run #57 was two builds of one rendering engine, and the claim it was cited for was cross-browser coverage. That is the same failure shape this file has recorded four times: a check whose output is true but whose *inference* is wrong.

**The correction was attempted, and it failed for an instructive reason (D-031).** Firefox was added as a CI matrix leg with an engine selected by name through `BROWSER_ENGINE`. **The leg failed on its first run, and the cause is not a bug here: Mozilla removed Firefox's CDP implementation.** Its own documentation for `remote.active-protocols` says that *"with the end of the CDP support, WebDriver BiDi is the only available protocol, and the preference was removed in Firefox 141."* This script speaks CDP, so no flag or path change can reach Firefox; it needs a WebDriver BiDi implementation, which is a different protocol rather than a configuration change.

This is where the engine-selection guard earned its place. Because a named engine that cannot be found **fails** instead of falling through, the leg reported "no browser found" and exited 1 **rather than measuring Chromium and reporting it as Firefox**. The failure was legible on the first run; a fall-through design would have shipped a green Firefox badge for a browser that never started. The unsupported engine now fails immediately with the reason printed, instead of timing out at the target lookup with "browser never exposed a page target", which reads as a flaky machine.

**The honest current claim is Chromium only** — two builds of it, Edge locally and Chrome/Chromium in CI. Gecko and WebKit are a known, named gap.

A second caveat of the same kind, still open: **nobody has timed any curriculum task.** Every band and every energy value is an authored estimate, not a measurement, and the UI says so on the page. This pass added 21 more of them.

This file deliberately carries no commit hash or commit count: a checkpoint
cannot contain its own hash, so those numbers drift by one commit on every
edit. For the exact current state, run `git --no-pager log --oneline -n 1`.

## Structure

```text
.
├── .editorconfig
├── .gitattributes
├── .gitignore
├── .github/                 (CI workflow — D-008)
├── README.md
├── AGENTS.md
├── CHANGELOG.md
├── CONTRIBUTING.md
├── LICENSE
├── docs/
├── scripts/                 (content tooling)
├── learning-site/           (React + Vite; 10 pages, 19 components, 13 hooks;
│                             generated/ and dist/ ignored)
└── career-roadmaps/
    ├── README.md
    ├── it-roadmap/          (9 phases + overview + checklist)
    ├── cybersec-roadmap/    (15 phases + overview + checklist + extras)
    ├── advance-roadmap/     (7 phases + overview + checklist)
    └── shared/              (3 files)
```

## Health checks

All of the following were re-verified against a **from-scratch rebuild** — `generated/` and `dist/` deleted, then `npm run build` — so they describe what a fresh session reproduces, not a stale working tree.

- [x] All content files recovered byte-for-byte from Git objects.
- [x] No literal `?` substitutes remain except three genuine question marks.
- [x] All files valid UTF-8; no CR bytes; no BOM; no mojibake in any edited file (verified at byte level, since a PowerShell console misrenders correct UTF-8).
- [x] `.gitattributes` and `.editorconfig` in place.
- [x] Remote configured. **The pass landed in `3d637e7`, and the `shared.json` contract correction in `94fa18c`; nothing is unpushed.**
- [x] `lint-content.mjs` — **162 files, 0 issues**.
- [x] `audit-content.mjs` — 0 issues across 31 phases.
- [x] `audit-lesson-ast.mjs` — all 31 lessons parse with no content loss.
- [x] `audit-readability.mjs` — **4 of 31** phases outside target (advance 03/04/05 at 19/21/19 and cyber 15 at 21, against the ≤ 18 target, all on sentence length only); **0 paragraphs over the 110-word ceiling, 0 over the 90-word editorial target**.
- [x] `audit-refs.mjs` — 0 broken cross-references, 2 informational forward-as-prior observations, and the detector's **10 controls passed this run** (6 must-fire, 4 must-NOT-fire). The `(0)` this audit reported for its whole prior life was **not a clean corpus**: its call site sliced four characters from the match start, so the cue regex could never fire.
- [x] `audit-terms.mjs --self-test` — **16 controls, 0 failed** (6 must-fire, 10 must-NOT-fire). Gating, and it checks the *detector* only; the corpus report always exits 0 by D-022.
- [x] `test:render-inline` 34, `test:highlight` 37, `test:ui` 42, `test:data` **86**, `test:notes` 28, **`test:work` 31**, **`test:today` 77**, `test:lesson-search` 77, `test:search` 41 — all pass.
- [x] `build-content.mjs` — **273 practice tasks banded and 273 carrying energy, 0 still minted from position**; 385 total phase task IDs (IT 90, cyber 179, advance 116), 31 lesson files.
- [x] `npm run test:smoke` — **231 renders** across 31 phases and 275 tools, 0 failures.
- [x] `npm run test:ui` — 42 checks across pace arithmetic, phase navigation, shortcut targeting and section-key namespacing.
- [x] `npm run build` — clean; initial chunk **556 KB raw / 162 KB gzipped** with 29 on-demand lesson chunks, **2,569 KB** of lesson JSON, `search.json` **673 KB** and `shared.json` **20 KB**.
- [x] 385 phase task IDs (IT 90, cyber 179, advance 116), all globally unique — the build prints the per-track counts itself.
- [x] CI is **green on `11f62e4`** — re-checked through the GitHub API: run **#57** on `main`, `push`, `completed / success`, and **both** jobs completed. The **Content integrity** job shows **every step `success`**, including the new **`Audit acronym detector (self-test)`** step at number 10, which is that step's first observation executing on a runner rather than merely being written. The **Learning site** job shows **`Browser check (real engine)` as `success`** — the observation task 4 needed, because it is the CI runner's Chrome and not this machine's Edge, so the width band and the engine identity are now confirmed on a second engine. Earlier green runs: **#55** on `23cc042` (both jobs — the run that first carried the acronym step), **#52** on `be40bfa` (every step in both jobs, the first observation of `Audit time budgets`), #53 on `51bb5f0`, #51 on `a07e7d1` (the first with the time-budget step), #46 on `b93e3f7`, #38 on `34f7ee3`, #37 on `94fa18c`. The site is live at <https://markkramm.github.io/cs-roadmap/>.

## Resuming

Read this first.

1. **Run the new guards before touching curriculum text or the site.** `node scripts/audit-refs.mjs` fails on a broken `Part N` / `Phase N` reference, `node scripts/audit-time-budget.mjs` fails when a stated total disagrees with its own parts, frontmatter or track overview, and `npm run test:browser` (with a preview server up) renders the six views in a real engine. All three run in CI. `scripts/audit-terms.mjs --self-test` also runs in CI and gates — but it gates the **detector**, not the corpus. The plain `node scripts/audit-terms.mjs` is still a **measurement that always exits 0**, and it still needs a human to read it. Do not wire the plain form into CI: its pass path is indistinguishable from a step that never ran.
2. **Read the acronym report knowing what it cannot see.** It recognises six shapes for "this term is explained here", including the bold-then-parenthesis form, a noun between term and gloss, and an expansion in the next table cell (tested by its initials, not its shape). It does **not** recognise a lowercase gloss — `**PID** is the process ID` is still reported — because the `X is a Y` pattern requires a capitalised gloss so it cannot accept a description like "The API is a black box". After this pass the actionable tier is **73**, down from 82, entirely from false findings being removed rather than from content changing.
3. **Rebuild before running the site.** `learning-site/src/data/generated/` is gitignored, so a fresh clone has no lesson JSON until `npm run build:content` (or `npm run dev` / `npm run build`, which both call it) has run.
4. **Run the guards after touching the parser.** `node scripts/audit-lesson-ast.mjs` is the one that matters; see [`WORKFLOW.md`](WORKFLOW.md) → "Guards on the lesson renderer".
5. **Beware inline `node -e` on Windows.** Backticks inside a PowerShell double-quoted string are mangled, which silently corrupts escape-sensitive regexes. Write a throwaway `.mjs` file instead — this cost two wrong measurements in one session.
6. **Beware PowerShell redirection when capturing a file for comparison.** `git show HEAD:file > out.md` writes **UTF-16LE** in Windows PowerShell 5.1, so a naive before/after word count reports a huge phantom loss. Use `git diff --word-diff` or `git show ... | Out-File -Encoding utf8`, and confirm the captured file has no BOM before trusting a number. This produced a false "3,131 words lost" during the paragraph pass.
7. **`CHANGELOG.md` now has a structure guard, and it found a real defect on its first run.** `[Unreleased]` accumulated eleven sections — four `Fixed`, four `Changed`, three `Added` — over at least four commits without anything failing, because Keep a Changelog permits one of each per release; they were merged mechanically in `b9f6133`. **The same shape returned twice more on 2026-09-16**, each time because a new entry was appended above an existing section of the same name rather than into it. `node scripts/audit-changelog.mjs` now gates three structural classes — a duplicate section heading, headings out of canonical order, and an empty heading or version block — with **13 controls** in `test-audit-changelog.mjs` beside it in CI (D-034). It immediately found `[0.1.0]` listing `Fixed` before `Changed`, and it was verified by reproducing the historical defect rather than only by asserting the fixtures pass. **It gates where the topic-list guard could not** (item 8): "does this heading appear twice" is arithmetic on the document, so a failing result is distinguishable from a passing one *by the check itself* — which is the test to apply before writing the next guard.
8. **No guard reads a phase's `## Specific topics to learn` against its lesson, and that is now a settled negative result rather than a missing guard.** Phase 2 promised malware removal in its topic list and never taught it; the word appeared once in the file, in that list — and the fourth comprehension pass found five more such bullets in the same file. `audit-content.mjs` checks that the *section* exists, not that every bullet in it is delivered, so a promise can be made and silently dropped. **A guard for this was investigated on 2026-09-16 and cannot be built** (D-033): tested against the five known-undelivered topics, a keyword probe returned hits for **all five** (a 0% detection rate), and a structural check fired 125 times on generic words. The reason is structural — probing `Startup apps` matched the topic list, the skills list, and a *practice task*, three promises and no teaching, so a presence-based check counts a promise as delivery and its pass path is the same shape as its skip path. **Do not spend another attempt on it.** The five defects were closed by writing the content, and the mechanism that finds the class is a reader using the topic list as a checklist — which is exactly what the comprehension pass does.

## Open items

- [ ] **The `FORWARD_AS_PRIOR` and acronym *corpus* classes print without gating, so each still needs a human.** Both detectors now carry controls that run on every invocation and gate (D-028), so the *instrument* is a real check. What is not a gate is the *judgement*: whether a forward phase cited as prior knowledge is acceptable, and whether a beginner can decode `SSID` unaided. By D-022 those are claims about the reader's state, not about the text agreeing with itself, so they stay prompts for a periodic read — and a periodic read needs someone to do it. **The part that changed this pass is that the prompts no longer lie about what they found.**
- [x] **The browser check runs on two engines and covers the 561–860px band — done 2026-09-16.** CI run **#57 on `11f62e4`** shows `Browser check (real engine)` as `success` in the Learning site job, driving the same checks over the same seven widths that Edge drives locally. **But this item was closed on a weaker claim than it reads as.** Both "engines" were Chromium, so a defect appearing only in Gecko or WebKit remained invisible to every guard. An attempt to fix that by adding Firefox as a CI matrix leg was made and **failed**, for a reason worth recording: **Mozilla removed Firefox's CDP implementation.** Its documentation states that *"with the end of the CDP support, WebDriver BiDi is the only available protocol, and the preference was removed in Firefox 141"* — so this script's protocol cannot reach Firefox at all, and the leg was removed rather than left red or made optional. **The honest current claim is Chromium only**, across two builds (Edge locally, Chrome/Chromium in CI). Gecko and WebKit are a known, named gap; closing it needs a WebDriver BiDi implementation or a WebKit runner, either of which is real work rather than a configuration change.

- [x] **The comprehension audit's verdicts were tested by a second pass, and they did not reproduce — which is the finding.** Done 2026-09-16. Eight fresh readers re-read all 23 phases with no sight of the first reports, producing roughly ninety further fixes across sixteen phases, and **two of the 23 verdicts changed**. IT 02's `would abandon` was over-called (one domain-tooling ticket is not a wall); IT 08's `followable with friction` was under-called. That is a two-for-two miss rate on the strongest verdict either pass can issue, from readers given the same brief on the same files — so the *findings* remain falsifiable and the *verdicts* are now recorded as advisory in [`COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md) → "Second pass — the re-review". What is still open is the thing no re-read can settle: see the time-budget item below.
- [ ] **21 more practice tasks now carry an unmeasured time estimate.** Every time budget in the repository is an author's estimate, and this remains the largest untested claim in the project. Each pass has made it sharper rather than softer, and this one widened it: cyber 15 and advance 07 added 21 banded tasks with no measurement behind them, taking the total to 273. The arithmetic is consistent everywhere, which is *not* the same as the estimates being right. Closing it needs one reader with a timer and a single phase, not more editing.
- [x] **Split the remaining dense paragraphs, and tighten the gate behind them — done 2026-09-16** (D-024). With the backlog at zero, `audit-readability.mjs` moved its hard gate from 150 to **110** — `EDITORIAL = 90` is reported and never gates, `CEILING = 110` fails. Result: **0 over 110 and 0 over 90** across all 31 phases. The two phases added since hold the target: cyber 15's two over-target interview paragraphs (97 and 95 words) were split at sentence boundaries, and advance 07 was written to it.
- [ ] **Five small follow-ups from the module review** — each closes a gap the review identified but deliberately did not fill. Listed in [`ROADMAP.md`](ROADMAP.md) → "Follow-ups from the module review".
- [x] **Migrate practice-task ids from minted to authored — done, and it went further than the id.** `build-content.mjs` no longer mints any id: all **273** practice tasks author `<!-- id: … band: … energy: … -->`, and the build reports `0 still minted from position`. Both phases added this pass authored theirs from the start. See D-019, D-021.
- [x] **Exercise the new views in a real browser — done 2026-09-16, and it runs in CI.** The notes UI, the dashboard rail, the print stylesheet, the Shared view, the Your work view and the time-budget control were rendered by `browser-check.mjs` — no new dependency. It asserts computed CSS, layout geometry, media-query behaviour, focus survival across keystrokes, and both real writes through their actual `localStorage` keys. **It found no site defects.** See "Rendered in a browser" above and D-023. It now covers **92 checks** across nine areas (93 when an engine is named, as CI runs it), including the two views added since and the phase-transfer panel. **It verifies Chromium only**: an attempt to add a Firefox leg was made this pass and removed after it failed, because Mozilla removed Firefox's CDP implementation (D-031). Gecko and WebKit are a known, named gap.

### Closed in this pass

- [x] **Two new phases, and both were written to the standard rather than to a word target.** **Cyber 15 — OT and ICS Security** closes the one target role the core path named and no phase taught, and it is the specialism with fewest people in it. **Advance 07 — Detection as Code** extends the advance track along the axis separating a senior detection engineer from an analyst. Both $0, both with explicit safety or authorisation boundaries, both authored with their task bands and ids rather than minted. Neither was padded to reach a length: cyber 15 came in ~1,100 words over the brief and the excess is covered material, not filler.
- [x] **A phase can now travel on its own** (D-030). `exportPhase` narrows a backup to the ids one phase owns, and **the emitted file is a real backup** — same `format`, same `version` — so the existing `inspect`/`importAll` read it with no special case. That is the property that makes it safe: a phase file cannot drift from a full backup, because it is the same document with a narrower `data`. `importPhase` is **additive by construction** and can never remove or overwrite. Guarded by `test:phase-transfer`, **56 checks** weighted toward rejection and merge cases.
- [x] **The engine-selection guard was written, and Firefox was attempted and removed** (D-031), closing the gap the previous checkpoint named as "two engines, not a cross-browser matrix" — which was generous, because both were Chromium. `BROWSER_ENGINE` selects by name and **a named engine that cannot be found exits 1**, which is the part that earned its place: when the Firefox leg was added and failed, it reported "no browser found" rather than measuring Chromium and calling it Firefox. **Firefox cannot be driven by this script at all** — Mozilla removed its CDP implementation, and its documentation states that with the end of CDP support, WebDriver BiDi is the only available protocol and the preference was removed in Firefox 141. The leg was removed rather than left red or made silently skippable, and an unsupported engine now fails with the reason printed. Grew 79 → **93 checks**.
- [x] **"Where you've been" and its structural no-shame rule.** The roadmap recorded this as deliberately unbuilt because it risked a completion-scold. The scold is avoided structurally rather than by care: `lib/pathOrder.js` rows carry **no `remaining`, `percent` or `missing` field at all**, so the absence is a property of the data and not of the rendering. Asserted twice — in `test:path-order` (37 checks) and in a live DOM by the browser check, which also greps the rendered text for scolding language.
- [x] **The shared documents are linkable by name.** The roadmap recorded the gap as "reached only by clicking the sidebar entry and then a chip". `Shared` now receives `initialId` as a seed and reports choices back through `onSelect`, because it remounts on every view change and local state would forget. The dashboard rail carries a "Beside the tracks" card reading from `shared.json`. Still not routing — no URL, no history entry (D-007 stands).
- [x] **A real inversion caught by a test written for it.** `importPhase` spread `{ ...existing, ...incoming }`, so importing silently **replaced** the note the reader could see with a staler one from a file — invisible in the UI, which reported success either way. The comment above the code already said "existing wins"; the code did the opposite. `test-phase-transfer` asserts the documented rule, which is what caught it.
- [x] **Two overviews and both master checklists did not know about the new phases.** Both said "six" in the heading, the count, the take-two-or-three advice and the total weeks; both checklists were missing the sections. Totals corrected and consistent with their own phase tables (**39 weeks** for advance), and the checklist's closing line now reads "one hundred and seven items across seven phases" — **counted, not asserted**.
- [x] **The CHANGELOG's duplicate sections were merged again, by script rather than by hand.** `[Unreleased]` had accumulated two `Added`, two `Changed` and two `Fixed` sections — the same drift `b9f6133` fixed once before, and which no guard reads. Merged mechanically into one of each in canonical order: **335 content lines before and after, compared set-wise and identical**, so the only thing removed was duplicate headers.
- [x] **The comprehension audit's own coverage note was stale in both directions.** It opened by saying the advance track was uncovered while the file already contained a "Third pass — the advance track" section. Corrected, and it now names what is genuinely unread: **the two phases added this pass**.

### Closed in the IT gap pass

- [x] **Nine IT topics that were missing or too thin, all folded into the phase that already owned them** (`docs/IT-CONTENT-AUDIT.md`). An audit of all 12 IT files (~143,000 words) against real entry-level remote requirements found **4 missing and 5 thin topics, and disproved 11 others** — several of which turned out to be covered better than commercial material (a full VPN ticket with MTU and split tunnelling; priority derived from an impact×urgency grid; a printer-driver refusal that teaches more than an install would). The blind spot was one thing: **a Windows-plus-Linux track in a market that now expects macOS and cloud-managed devices.** macOS had 2 real mentions in 143,000 words; MDM, VoIP, mobile/BYOD and docking stations had none. **No tenth phase** — every addition went into an existing one, all still $0. IT checklist **66 → 75 items**, one per gap.
- [x] **A promised topic in Phase 2 that the lesson never delivered.** `02-phase-operating-systems.md` listed "Windows Defender and basic malware removal steps" under `## Specific topics to learn`, and the word "malware" appeared in the whole 9,954-word lesson **exactly once — in that list**. Now taught: the four things people call a virus, Defender's quick/full/offline scan, quarantine, and when rebuilding beats cleaning. This is the same defect class as the acronym and forward-reference detectors — **a claim in the text that the text does not keep, which no guard can see, because nothing in the file disagrees with anything else in it.**
- [x] **Phase 4's part numbering skipped 8 and 9, and Phase 3 broke its own sequence.** `git log` traces Phase 4 exactly: Parts 8 and 9 were "Key takeaways" and "Practice this next", and commit `14a70c6` appended five new teaching parts as 10–14 and dropped the old closing parts without renumbering. Now 8–12, plus **all 13 cross-references** to them elsewhere in the file. Phase 3's unnumbered `### Common pitfalls` H3, which split Parts 5 and 6, is now an H4 inside Part 5 — matching how Phase 4 already had it. `PART_GAP` passes on both.
- [x] **A wrong fix, caught by the repository's own guard rather than by review.** The audit also claimed Phase 4's `Key takeaways` was misordered and proposed renumbering the drill to fix it. That was wrong: `scripts/audit-content.mjs` enforces `CLOSING_ORDER` — **`Key takeaways` must precede `Practice this next`**. The suggested reorder was implemented, the guard failed it, and it was reverted. The numbering gap was the only real defect in that file.
- [x] **The audit report was moved out of the study directory and its own errors recorded in it.** It was written to `career-roadmaps/it-roadmap/AUDIT-content-gaps.md`, mixing meta-documentation into material a reader is meant to read straight through; it now lives at `docs/IT-CONTENT-AUDIT.md`. Its two counting errors (`dock` reported 0 but is 10, all Docker; `Intune` reported 2 but is 6, five in the *cyber* track) and its wrong 5.2 diagnosis are corrected in the file under "Where this report was wrong", because a report that lists only its successes is not an audit. It also contained the repository's **only** invalid UTF-8 sequence — a quoted mojibake artifact — which is now described rather than reproduced.

### Open items carried into the next pass

- [x] **Three phases have now never been read by a fresh reader, not two — CLOSED 2026-09-16.** Cyber 15 and Advance 07, plus the materially expanded IT phases, were all read by fresh readers this pass. **~40 findings, about a dozen rejected, fourteen real fixes.** The advance track sat in exactly this position before its third pass, and that pass found 45 findings; this one found fewer, and the rate of false positives was higher (near one in three against one in five). See [`COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md) → "Fourth pass — the unread material".
  - **The market claim is now the remaining exposure, not the reading.** The IT additions are the only material in the repository written to a **market claim** — that remote entry-level work now expects macOS and cloud-managed devices — rather than to an existing syllabus. They now read correctly and one false fixture of reasoning was fixed (the dock test's over-claim), but **nobody has yet checked the claim against a real job posting from the target market.** That is an external check no agent in this session could perform: `web_search` returned HTTP 401 (invalid API key, a settings issue rather than a repository one) and Indeed blocks automated fetching. Closing it needs either working search credentials or a human reading twenty postings.
- [x] **Five IT 02 topics are listed in `## Specific topics to learn` and are still not delivered — CLOSED 2026-09-16.** All five are now taught: dependent services, startup apps / slow-boot diagnosis, NTFS versus share permissions, update rollback, and profile corruption. Phase 2 grew **12,272 → 15,431 words**, with five practice tasks (**385 → 390** task IDs), five checklist items (**7 → 12**), and three new deliverable artefacts. See [`CHANGELOG.md`](../CHANGELOG.md) → Added.
- [x] **A guard over `## Specific topics to learn` was investigated and is NOT buildable — recorded as a negative result rather than left as an open task.** `ROADMAP.md` called this "the highest-value guard currently absent", and this session tested it against ground truth the comprehension pass had already established (the five IT 02 topics known to be undelivered). **It failed on every approach:**
  - **A keyword probe for the five known-undelivered topics returned hits for all five** — a false pass on 5 of 5 true positives.
  - **A "does this phrase appear only in the topic list" check fired 125 times** across 24 phase files, mostly on generic words (`changelog`, `square`, `around`, `drives`).
  - **Inspecting the hits explained why.** "Startup apps" matched the topic list, the skills list, and a *practice task* — with no teaching anywhere. Any presence-based check counts that as delivered. **The guard would print green on precisely the defect it exists to catch.**
  - The class is the same one `audit-terms.mjs` failed to converge on: "is this topic taught" is a judgement about whether prose constitutes teaching, not a property of a string. See [`DECISIONS.md`](DECISIONS.md) → D-033. **The lesson is that a guard whose pass path looks like its skip path is worse than no guard**, and this repository has been burned by that four times.
- [ ] **The nine IT gaps are closed; the audit that found them is not fully re-verified.** `docs/IT-CONTENT-AUDIT.md` records four MISSING and five THIN topics, all now taught, and eleven suspected gaps it disproved. A second pass over the *new* text has now happened (see the closed item above), which is what this item was waiting on. What has still not happened is a check of the *market claim itself* against real postings — see the sub-item above.
- [ ] **Firefox was attempted and failed; the failure is now part of the record rather than an open task.** The Gecko path is a recorded dead end: Mozilla removed Firefox's CDP implementation, so this script's protocol cannot reach it, and the CI leg was removed after it failed on its first run. Closing it means implementing WebDriver BiDi — real work, not a configuration change. What the attempt *did* prove is that the engine-selection guard works: the leg failed loudly instead of measuring Chromium and reporting it as Firefox.
- [ ] **Gecko and WebKit remain absent**, named rather than implied. Gecko cannot be driven by this script at all, because Mozilla removed Firefox's CDP implementation (D-031) — closing it needs a WebDriver BiDi implementation. WebKit has no headless build a runner can install without a dependency rule 3 forbids. **The honest claim is Chromium only**, across two builds. That is a real and named gap, not coverage.
- [ ] **Nobody has timed a single curriculum task, and five passes have now added more of them.** The gap is wider than it was: **283 practice tasks** carry authored bands, and not one has been measured. Closing it needs one reader with a timer and a single phase. **This is now the oldest open item in the repository and the only one no pass can close** — every comprehension pass has recorded it, and every one has left it exactly where it was. D-021 already records that bands are *estimates* by design, which softens the cost but does not close the gap: an estimate nobody has ever checked is still an estimate.
- [ ] **The acronym corpus report still needs a human**, by design (D-022). It now emits **272 domain terms**, down from 286 after the classifier was fixed, and still cannot distinguish a term a beginner can decode from one they cannot. The `FORWARD_AS_PRIOR` list (2 observations) is in the same state — printed, not gated, because the guard was wrong about it once already.
- [ ] **A corpus-level quiz balance guard is specified in [`CONTENT-SCHEMA.md`](CONTENT-SCHEMA.md) but not built.** Today `audit-quiz.mjs` balances each phase in isolation, and the corpus total (**A=90 B=97 C=97 D=96**) is reported without being gated. The per-phase rule is the one that matters — a reader answers one phase at a time — so this is a refinement rather than a hole.
- [ ] **One IT 02 topic remains defined but not taught: "local user vs Microsoft account".** The five genuinely missing topics from this pass's topic-list check are now written (see the closed item above). This one is *partial*: both account types are defined at the topic-list and permission-model level, but nothing teaches how a Microsoft account is created, how to convert local → Microsoft account or back, or what breaks when one is lost — which is the first-line ticket the topic exists for.

- [x] **Audited all 23 phases for beginner comprehension, and fixed five structural classes of defect.** See the "Comprehension audit" row above and [`COMPREHENSION-AUDIT.md`](COMPREHENSION-AUDIT.md). 18 phases needed at least one fix; cyber 07 came back clean and was checked rather than trusted; IT 02 was judged "would abandon". Three subagent claims were rejected as wrong after re-reading the source.
- [x] **Added a cross-reference guard, because the audit proved exactly one class is machine-checkable** (D-022). `scripts/audit-refs.mjs` exits 1 on a `Part N` reference to a heading the file does not have, or a `Phase N` reference to a number the track does not contain. It found the IT 06/IT 07 defects, then independently confirmed `cybersec 11:64`. `FORWARD_AS_PRIOR` prints but does not gate — the guard was wrong about it once already.
- [x] **Rendered the six never-verified views in a real browser, with no new dependency** (D-023). `learning-site/scripts/browser-check.mjs` drives Edge (locally) and Chrome (in CI) over the DevTools Protocol using Node 24's global `WebSocket`. **79 checks, 0 failed** as of this pass (50 when D-023 was written; the seven-width band added 29). It found no site defects, and its own three first-run failures were its own fault — recorded rather than quietly fixed.
- [x] **Stopped a checklist row claiming a paid feature was free.** `checklist-master.md` said "I enabled MFA and a conditional access policy in a free cloud tenant"; Phase 09 says at line 376 that conditional access needs Entra ID P1.
- [x] **Surfaced the shared documents, and made the reader's writing readable back** (D-020). `career-roadmaps/shared/` had no path to it from the site at all; a `Shared` view now renders all three documents from a new `shared.json` (3 documents, 40 blocks, 42 resources, 20 KB) built by `scripts/shared-content.mjs`, which reuses the lesson parser via a `headingBase` parameter rather than forking it. A `Your work` view gathers every note and every answer across all 23 phases onto one page, matching answers **by id and never by position**, keeping orphans, and carrying **no denominator** at all — `summariseWork()` has no `total`/`percent`/`remaining` field, so the absence is structural rather than a rendering choice. Guarded by `test:work`, 31 checks.
- [x] **Gave every task a duration, and asked the reader how long they have** (D-021). All 252 practice tasks now carry an authored band and energy value, and the dashboard draws its suggestion from practice tasks rather than the checklist. `ongoing` is an exclusion path, `fitsBand` fails closed, and a real defect was caught by the new test: an unaddressed `ongoing` task used to fall through to `all-addressed`, telling a reader **"nothing left" while they still had work**. Guarded by `test:today`, 77 checks. The time budget is a tenth `localStorage` key, and the backup registry grew 9 → 10.
- [x] **Rewrote the storage-key guard so it can fail.** See "The storage-key guard was rewritten" above — it compared the test to itself and had broken twice while catching nothing.
- [x] **Gave the reader somewhere to write** (D-019). A note per phase and an answer per practice task, in a ninth `localStorage` key. Deliberately unscored, and asserted to stay that way. Practice tasks gained stable ids for it, which they had never had.
- [x] **A guard that failed silently, found by adding a key.** `test-data.mjs` hardcoded “eight keys” and seeded eight, so registering the ninth made its round-trip loop call `JSON.parse(undefined)` — which threw a stack trace and took the whole suite down, reporting **nothing** rather than reporting a failure. It now reports a missing fixture value as a failed check. This is the second time in this project that a guard printed nothing and was read as green; the first was the `^0[1-9]-` pattern that hid phases 10+.
- [x] **Split every dense paragraph, and gated the audit so they stay split.** 33 paragraphs over 90 words → **0**. The old gate measured a per-phase *average*, which a single wall of text does not move.
- [x] **Audited modules 09–14.** Structurally sound, no restructuring needed, but **five real technical errors** found and fixed that no automated check could catch: a reversed auditd privilege claim, a silently-wrong AWS version-id assumption, a `Get-ScheduledTask` `.Date` string-comparison trap, a retired `sigmac` reference, and a misleading Sysmon filename. Full detail in [`ROADMAP.md`](ROADMAP.md).
- [x] **Assessed the IT track's later phases. Finding: no depth pass is needed.** Phases 6–9 are 6,457–6,992 words across 8–10 substantial Parts — complete lessons, not stubs — and the depth signature (guided walkthrough or worked case) is already present in Phases 2–5 and 8. Phase 1's 24,723 words is the outlier in the other direction and is not a template. The real IT-track defect is **paragraph density, not depth**.
- [x] **Wrote the five module-review follow-ups.** A worked "wrong first guess" and a five-rung setup diagnostic ladder in module 09, a reversed wrong-automation case in module 12, a worked risk-scoring disagreement in module 13, and a residual-incompetence statement in all six modules — each naming a real limitation rather than a humble generality. 366 lines added, nothing deleted.
- [x] **Added full-text search.** See the Search row above and D-013.
- [x] **Confirmed the site is deployed.** It was already live — see the Deploy row above. No new hosting was needed.
- [x] **Gave the reader somewhere to go** (D-014). The phase page had no "next"; a 15,500-word lesson reported no depth and offered no place to stop; "0/90 · 0%" had no reference point. All three were consequences of the same thing the earlier work established — the lesson is 84–95% of a phase file — so the application had grown a reader without growing the affordances a reader needs. Closed with prev/next phase navigation, per-section done toggles stored separately from the checklist, a lesson-scoped reading bar, a resume prompt that suggests rather than jumps, a Schedule view, keyboard shortcuts with none bound to a checkbox, a reading-size control, and a 560px breakpoint.

### Paragraph density, current state

- **Nothing exceeds the 110-word hard gate anywhere in the 31 phases**, and **nothing exceeds the 90-word editorial target either.** The gate moved from 150 to 110 in D-024, after the IT backlog was cleared. The longest paragraph in the repository is now **under 90 words**; the previous longest was 108 (advance 05-adversary-emulation), and the longest in the two original tracks was 90 (IT) / 86 (cyber). The two phases added this pass were written to the target from the start and needed no splitting for density — though cyber 15 needed two interview paragraphs split at 97 and 95 words to clear it.
- **The 16 over-target paragraphs in the advance track were split this pass, and the split was proved mechanical rather than asserted.** `audit-readability.mjs` gained a `--list` flag that prints each over-target paragraph with its file and line, because the report had given a count with no way to act on it; the refactor that added line tracking preserved the count exactly (17 before, 17 after, exit 0), which is the check that it changed the reporting and not the measurement. Every split kept **every word**: `6eda6d7..d62b204` is byte-identical in word sequence across 35 files and 405,911 words.
- **The gate sits at 110 rather than at the 90-word editorial target, and that gap is deliberate.** A gate at 90 would fail on every clarification a writer adds. 110 fails on prose a reader has to re-read and passes prose that merely wants splitting: the failure that keeps a gate honest is one it can pass.

## How to verify quickly

From the repository root:

```powershell
git --no-pager log --oneline -n 5
git --no-pager status --short          # expect the files this pass touched, then empty
git --no-pager log origin/main..HEAD --oneline   # expect empty after pushing

node scripts/lint-content.mjs            # content integrity
node scripts/audit-content.mjs           # phase structure across all 31 phases
node scripts/audit-lesson-ast.mjs        # parser loses no lesson content
node scripts/audit-readability.mjs       # prose density targets
node scripts/audit-refs.mjs              # every Part N / Phase N reference resolves
node scripts/audit-time-budget.mjs       # stated totals match their own parts, frontmatter and overview
node scripts/audit-terms.mjs --self-test # acronym DETECTOR — 16 controls, gates
node scripts/audit-terms.mjs             # acronym CORPUS — measurement, always exits 0
node scripts/cyber-restructure-check.mjs # no cyber phase shrank

cd learning-site
npm run build                            # production build
npm run test:smoke                       # render every phase with real data
npm run test:ui                          # pace, navigation, shortcuts, section keys
npm run test:data                        # backup export/import, incl. the rejection cases
npm run test:notes                       # the notes store and its merge rule
npm run test:work                        # every note and answer reads back, orphans kept
npm run test:today                       # bands, fitting, addressed rules, every reason
npm run test:lesson-search               # in-lesson find agrees with the corpus engine
npm run test:phase-transfer              # one phase exported and merged, incl. rejection cases
npm run test:path-order                  # the three states, and the no-denominator rule
npm run test:search                      # index and query engine agree
npm run preview -- --port 4173           # in one terminal, then in another:
BROWSER_CHECK_STRICT=1 npm run test:browser   # render every view in a real engine
```

To check a specific engine by name, set `BROWSER_ENGINE=chromium` first. **A named engine that cannot be found fails the run even without strict mode**, because a run that asked for one engine and quietly measured another would be cited as cross-browser evidence for an engine that never started. **Do not set `BROWSER_ENGINE=firefox` expecting a run**: Gecko is a recorded dead end (D-031) — Mozilla removed Firefox's CDP implementation, so the script now fails immediately and prints why rather than timing out at the page-target lookup.

On Windows PowerShell, set the variable first: `$env:BROWSER_CHECK_STRICT = "1"` then run `npm run test:browser`. Without it, no browser means a skip and exit 0 — which is honest locally and is exactly why CI sets it.

Note: `cyber-restructure-check.mjs` exits 2 on a fresh clone because its baseline lives in `TEMP`. That is expected and it is why CI does not run it.

Expected results: content audit `0 issues`; encoding `71 files, 0` with 16 controls; lesson AST `31 lessons, 0 loss`; readability `1 paragraph over the 90-word editorial target, 0 over the 110-word gate`; cross-references `0 broken`; time budgets `31 phases checked, 0 findings`; acronym detector `16 expansion + 23 classifier controls, 0 failed`; quiz `0 findings / 380 questions, corpus A=90 B=97 C=97 D=96`; quiz sourcing `0 findings / 380, 15 controls`; changelog `0 findings, 13 controls`; framework-claims `41 files / 9 rules, 31 controls`; nist-current `10 checks, 0`; commands `0 findings`; build `283 banded, 0 authored without a band, 0 minted from position`; energy `283 of 283`; shared documents `6`; smoke `235 renders across 31 phases, 275 tools and 6 documents, 0 failures`; render-inline `34`, highlight `37`, ui `42`, data `103`, notes `28`, work `31`, today `77`, lesson-search `77`, phase-transfer `56`, path-order `37`, quiz `53`, review `44`, search `41` — all pass; browser `118 checks, 0 failed` (Chromium only; **needs a preview server already running** — `npm run build && npx vite preview --port 4173`, then `npm run test:browser`; the suite does not start one, so a dead server reports `timed out waiting for app shell to mount`, which looks exactly like a mount regression and is not one).